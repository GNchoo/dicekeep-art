import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';import{createHash}from'node:crypto';
const dir=path.dirname(fileURLToPath(import.meta.url)),file=path.join(dir,'parts-jobs.json'),jobs=JSON.parse(fs.readFileSync(file)),id='late-biped-02-identity-fix';
if(!jobs.some(j=>j.id===id)){
 const references=['sources/t8-biped-01-turnaround.png'].map(p=>path.join(dir,p).replaceAll('\\','/'));
 const prompt=`Create ONE TALL PORTRAIT 1:2 atlas, exactly FOUR ROWS x THREE COLUMNS. Eleven isolated rig parts, last cell blank. Match the three characters of the reference exactly, without mixing equipment between rows.
ROWS1–3 columns are RIGHT-facing pure SIDE, TRUE FRONT, TRUE BACK of the same legless character body. The body retains integrated head, torso, shoulders/arms/wings, hip garment, full tail and held weapons, but remove both walking legs below hip overlap; no standing feet or attached shins. Close the garment/fur underside, no holes or exposed cuts. Each remains one connected illustrated piece, no detached weapons. BACK is actually seen from behind, no face, reverse physical handedness correctly.
ROW1 green lizard spearman: bronze scale armor, small horns, spear in physical RIGHT hand and round bronze shield in LEFT hand. Thus FRONT spear appears on screen LEFT and shield RIGHT; BACK spear screen RIGHT and shield LEFT. Keep one green tail with pale underside curving behind hips. In the right-facing SIDE the nearer visible right hand holds the spear, the left-hand shield may overlap farther away. Tail must remain attached in every view.
ROW2 golden cockatrice: exactly a bird, rooster comb/beak, golden neck feathers, green body feathers, bronze breastplate and decorative shoulder plates, folded feather WINGS and long green rooster tail. NO human hands, no spear, no shield, no boots. FRONT looks straight at camera with centered beak and balanced eyes, BACK shows rear of head and full tail. SIDE tail curves behind body; both walking legs removed from the body.
ROW3 red dragonkin swordsman: bronze armor, red scaled head, sword held in physical RIGHT hand, broad shield in LEFT hand. FRONT sword screen LEFT and shield RIGHT; BACK sword screen RIGHT and shield LEFT, show both clearly and full red plated tail between/behind hips. No missing sword or shield in back. SIDE full connected tail extends behind, held sword points down-forward. Both walking legs removed.
ROW4: two SEPARATE complete cockatrice SIDE legs only, in columns1 and2, column3 empty. Each leg is feathered green upper thigh -> digitigrade green scaly shank -> three bird toes pointing RIGHT, matching bare legs in the reference, no armor/boots. Long relaxed leg with slight natural joint bend and closed feathered overlap root. Distinct near/far legs, not connected to a body.
All objects wholly inside cells, generous clear gutters, highest supported resolution, transparent background without drawn checkerboard. Approachable hand-painted game-character style, no photorealism, gore, red cuts, labels, typography, scenery or floor.`;
 const j={id,family:'biped',stage:'identity-correction',layout:'custom-11-parts',assetIds:['w074','w075','w079'],rows:[{assetId:'w074',view:'all'},{assetId:'w075',view:'all'},{assetId:'w079',view:'all'},{assetId:'w075',view:'side-legs'}],references,referencesSha256:references.map(p=>createHash('sha256').update(fs.readFileSync(p)).digest('hex')),prompt,promptFile:'prompts/'+id+'.txt'};fs.writeFileSync(path.join(dir,j.promptFile),prompt+'\n');jobs.push(j);fs.writeFileSync(file,JSON.stringify(jobs,null,2)+'\n');
}
console.log(id);
