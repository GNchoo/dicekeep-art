import fs from'node:fs';import{dir,measuredView}from'./biped-config-lib.mjs';const f=dir+'/t5-biped-01a-rigs.json',c=JSON.parse(fs.readFileSync(f));c.entries[1].views.side=measuredView('t5-biped-01a-side-front-parts',2,{name:'side',sockets:[[.45,.78],[.62,.78]],kneeY:.4,ankleY:.8});c.entries[1].views.side.jointReview+=' W42 pins the original atlas because the shield-only edit changed the unrelated far boot to front-facing; original W42 parts are correctly authored right-facing. W41 corrected torso still uses revised atlas.';fs.writeFileSync(f,JSON.stringify(c,null,2)+'\n');
