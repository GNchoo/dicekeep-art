import fs from'node:fs';const d='tools/art-review/directional-101/middle-waves',f=d+'/parts-jobs.json',j=JSON.parse(fs.readFileSync(f)),ledger=JSON.parse(fs.readFileSync(d+'/generation-ledger.json')),done=new Set(ledger.requests.map(r=>r.id));const extra=' IMPORTANT single-leg anatomy: leg components have NO waist, NO belt around a whole pelvis, NO full shorts, NO crotch, NO second foot. Draw only ONE narrow upper-thigh cuff and ONE pant leg ending in ONE boot. The torso is the ONLY object with a head or waist; it has ZERO legs or feet. Never replace an isolated leg with a pair of pants.';for(const job of j)if(!done.has(job.id)&&!job.id.includes('single')){job.prompt+=extra;fs.writeFileSync(d+'/prompts/'+job.id+'.txt',job.prompt+'\n');}fs.writeFileSync(f,JSON.stringify(j,null,2)+'\n');
