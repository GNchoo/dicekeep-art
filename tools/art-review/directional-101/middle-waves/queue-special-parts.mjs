import fs from'node:fs';
const d='tools/art-review/directional-101/middle-waves/',f=d+'parts-jobs.json',jobs=JSON.parse(fs.readFileSync(f)),catalog=JSON.parse(fs.readFileSync(d+'jobs.json')).flatMap(j=>j.entries);
const end=' Preserve exact individual color, outfit, mount, rider and equipment from the references. Full intact connected pieces, wide empty gutters between every object and row. Moderately stylized hand-painted fantasy tower-defense art, chunky shapes and expressive readable faces; no blood, injuries, rot, exposed flesh, detached gore, scary realism, pets or unrequested creatures. Smooth clean toy-like ivory for skeletal material. Transparent alpha background, no floor, shadow, scenery, gridlines, labels or text.';
const orientation=v=>v==='side'?'PURE RIGHT-FACING PROFILE: entire character head, muzzle, mount and tucked/isolated foot toes face RIGHT.':v==='front'?'TRUE FRONT facing viewer, front costume and face, front feet/toes toward page bottom. Physical left is on screen-right.':'TRUE REAR facing away: rear hair/helmet/mane/palm, back costume and heels, no front face or front toes. Physical left is on screen-left.';
const save=j=>{if(jobs.some(x=>x.id===j.id))return;jobs.push(j);fs.writeFileSync(d+'prompts/'+j.id+'.txt',j.prompt+'\n');};
for(const [base,ids,refs]of [['winged-mounts',['w044','w048'],['t5-mounts-01-turnaround.png']],['membrane-dragons',['w036','b060-2'],['t4-flight-01-turnaround.png','t6-boss-01-turnaround.png']]])for(const view of['side','front','back']){
 const rows=ids.map(assetId=>({assetId,view})),prompt='Use case: stylized-concept. Create a NEW isolated flight-animation PARTS ATLAS, landscape1536x1024, exactly TWO equal horizontal rows and THREE component columns. EACH ROW: LEFT THIRD one complete creature torso/head/neck/tail/tucked feet plus its rider when specified, with BOTH WINGS ENTIRELY ABSENT at closed shoulder contours; MIDDLE THIRD one SINGLE physical LEFT wing; RIGHT THIRD one SINGLE physical RIGHT wing. Exactly three disconnected painted objects per row. Wings are a single broad connected shoulder-to-tip panel each, not two wings joined, no body or extra foot on a wing. Keep rounded substantial shoulder roots for assembly. Both detached wings are drawn open, roughly downward and outward from their narrow upper shoulder root to wide lower feather/membrane fan. Draw the appropriate underside/upper-side feathers or membranes for this view; no attached wing stubs on torso. Body and wings at the same individual scale, large clean gutters, no overlap. '+rows.map((r,i)=>'ROW '+(i+1)+' ONLY: '+catalog.find(e=>e.assetId===r.assetId).description+' '+orientation(view)).join('\n')+end;
 save({id:base+'-'+view+'-parts',kind:'parts',assetIds:ids,rows,reference:refs[0],extraReferences:refs.slice(1),prompt});
}
for(const view of['side','front','back']){
 const ids=['w043','w054'],rows=ids.map(assetId=>({assetId,view}));
 const prompt='Use case: stylized-concept. Create a NEW isolated quadruped-animation PARTS ATLAS, landscape1536x1024, exactly TWO equal rows and THREE columns. LEFT HALF each row: complete HORIZONTAL four-legged animal torso with head, neck, tail and rider if specified, but ZERO attached legs below the shoulder/hip. Preserve natural shoulder and rounded rump contours as part of the torso, no hollow holes and no hanging leg stubs. MIDDLE QUARTER: ONE single FORELEG from narrow upper shoulder through elbow/carpal joint to one complete hoof/paw. RIGHT QUARTER: ONE single HINDLEG with small upper thigh, distinct forward stifle knee, backward hock, and ONE complete hoof/paw. Natural horse/dog proportions, upper limb nearly vertical, rear hock-to-paw curve intact. Each piece is one entire connected limb; no pairs of legs, pelvis, head or torso on a limb. Body and limb source scale consistent. '+rows.map((r,i)=>'ROW '+(i+1)+' ONLY: '+catalog.find(e=>e.assetId===r.assetId).description+' '+orientation(view)).join('\n')+end;
 save({id:'horse-puppy-'+view+'-parts',kind:'parts',assetIds:ids,rows,reference:'t5-mounts-01-turnaround.png',extraReferences:['t6-misc-01-turnaround.png'],prompt});
}
{
 const id='glider-three-view-parts',rows=['side','front','back'].map(view=>({assetId:'w032',view}));
 const prompt='Use case: stylized-concept. Create a NEW isolated GOBLIN GLIDER rig-parts atlas, portrait1024x1536 with exactly THREE equal rows and THREE component columns. Only the small green goblin pilot with goggles beneath ochre cloth glider from reference top row. Every row LEFT third: intact goblin pilot with tucked booted legs, complete arms gripping the central wooden harness and center struts, NO attached broad wing panels. MIDDLE third: ONE left ochre canvas wing panel and its short root spar. RIGHT third: ONE right canvas wing panel and short root spar. The left and right panels join separate ends of the central top harness when assembled; each is a single triangular cloth panel with intact wooden edge spars and substantial root connector. No complete double-wing glider on a component. '+rows.map((r,i)=>'ROW '+(i+1)+': '+orientation(r.view)).join('\n')+end;
 save({id,kind:'parts',assetIds:['w032'],rows,reference:'t4-flight-01-turnaround.png',prompt});
}
for(const view of['side','front','back']){
 const prompt='Use case: stylized-concept. Create a NEW single-row isolated animation parts atlas, landscape1536x1024. ONLY the oversized enchanted violet LEATHER WIZARD GLOVE with brass cuff and amber rune in reference middle row. Exactly SIX disconnected objects with large empty gutters: LEFT THIRD is glove cuff and palm body with all five LONG fingers removed, retaining only five short CLOSED stitched knuckle roots around the palm edge. Remaining two-thirds are FIVE individual glove fingers in order THUMB, INDEX, MIDDLE, RING, LITTLE. Every component is one continuous leather digit from rounded cloth root through middle bend and tip-base bend to ONE closed rounded padded fingertip. No human flesh, bones or gore: these are intact empty magical leather costume pieces. Each finger curves gently downward like a relaxed spider-walking glove, ready to support the palm on its padded fingertip; fingers must have distinct correct lengths, the thumb thicker/shorter and little finger shorter. No sixth finger, no complete hand attached to a finger, no pair of fingers fused together. The palm has no full fingers attached. '+orientation(view)+' Keep the same right-hand glove identity through views: front palm thumb root screen-left, rear dorsal thumb root screen-right; side shows consistent leather seams and cuff. Fingers match that view, no fingernails. '+end;
 save({id:'wizard-hand-'+view+'-parts',kind:'parts',assetIds:['w056'],rows:[{assetId:'w056',view}],reference:'t6-misc-01-turnaround.png',prompt});
}
fs.writeFileSync(f,JSON.stringify(jobs,null,2)+'\n');console.log(jobs.length);
