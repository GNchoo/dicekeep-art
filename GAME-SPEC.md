# 주사위 성채 (Dicekeep) — 게임 명세

최종 정리: 2026-09-02. 저장소: https://github.com/GNchoo/dicekeep-art

이 문서는 **현재 구현된 규칙·아트·파일 위치**의 단일 소스다.
이어서 작업할 때는 이 파일 → `content.js` → `game.js` → `ART-PROMPTS.md`(생성 프롬프트) → `GROK-HANDOFF.md`(옛 브리프) 순으로 읽는다.

대화는 한국어, 이미지 생성 프롬프트는 영어(Grok Imagine).

---

## 1. 한 줄 요약

주사위를 굴려 나온 눈(1~6)이 곧 타워 종류다. 눈을 빈 석단에 놓으면 설치, 같은 눈 위에 놓으면 합체(최대 Lv3). 포탈에서 나온 적이 크리스탈에 닿기 전에 막는다. **50 스테이지, 5 난이도 티어, 티어가 오를수록 적 동선(레인)과 석단이 늘어난다.** 인피니티(무한 투기장)는 이와 별개로 처음부터 열려 있다.

---

## 2. 실행

```bash
git clone https://github.com/GNchoo/dicekeep-art.git
cd dicekeep-art
python serve.py
# http://localhost:8137          게임
# http://localhost:8137/editor.html  맵 좌표 에디터
```

Windows: `start.bat`. **file://로 열지 말 것** (캔버스 tainted → 크로마키 실패).

캐시: `index.html`·`editor.html`의 `?v=N` 을 올릴 것 (현재 v48).

개발용 URL: `http://localhost:8137/?unlock=all` (50 스테이지 클리어·타워 전부 해금 상태로 시작, 젬 200 보장) · `?start=inf` / `?start=clear` (타이틀에서 바로 인피니티 무한/도전). 인피니티는 해금 조건이 없어졌으므로 `?inf=1` 은 하위호환용 무의미 플래그다.

디버그 (콘솔):
- `window.DK` 게임 상태 (`gold`, `enemies`, `towers`, `stageData` …)
- `window.DKA` 로드된 스프라이트
- `window.DKDIE` 필드 3D 주사위 (`forceFinal` 로 눈 강제)
- `window.DKSLOT` 버튼 슬롯 굴림
- `window.DKthrow(vx, vy)` 필드 물리 던지기
- `window.DKLANES()` 현재 레인 배열
- `window.DKstart(n)` 스테이지 n 바로 시작 · `DKstartInf()` 인피니티 시작 · `DKinf()` 런 상태 · `DKupgrade(f)` 눈 파워업(골드) · `DKchest()` 뽑기 · `DKenhance()` 확률강화 · `DKqueue()` 보상 대기열 · `DKTD` 타워 정의 · `DKhelp()` 안내 카드
- `window.DKCONTENT` 맵/적/타워/티어 카탈로그 (`buildLayout`, `tiers`)

---

## 3. 규칙

| 항목 | 값 |
|---|---|
| 굴리기 비용 | 40G |
| 목숨 | 20 |
| 스테이지 | 50 (맵당 1개), 10개마다 티어 상승 |
| 웨이브 | 스테이지당 8 ~ 17 (마지막 웨이브 = 보스) |
| 시작 골드 | 티어 기본값 + 스테이지×4 (S1 130 → S50 546) |
| 합체 | 같은 눈만, 최대 Lv3 |
| 젬 | 최초 클리어 8 + 스테이지/2 + (티어−1)×2, 재클리어는 1/3 |

### 난이도 티어

| 티어 | 스테이지 | 이름 | 레인 | 추가 석단 | HP 배율 | 마릿수 보너스 | 시작 골드 |
|---|---|---|---|---|---|---|---|
| 1 | 1~10 | 초원 | 흙길 | +0 | 1.00 | +0 | 130 |
| 2 | 11~20 | 언덕 | 흙길 + 하늘길 | +2 | 1.30 | +2 | 170 |
| 3 | 21~30 | 협곡 | 흙길 + 하늘길 + 땅굴 | +4 | 1.65 | +4 | 220 |
| 4 | 31~40 | 요새 | 흙길 + **흙길2** + 하늘길 | +6 | 1.85 | +6 | 280 |
| 5 | 41~50 | 악몽 | 흙길 + **흙길2** + 하늘길 + 땅굴 | +8 | 2.10 | +8 | 350 |

정의: `content.js` `TIERS`, `tierOf(n)`. 스테이지 객체(`stages[n-1]`)에 밸런스 값이 계산돼 들어 있고 `game.js` 는 그 값을 그대로 읽는다.

- `hpScale = 티어 HP 배율 × 1.02^(n−1)` (S1 1.0 → S50 약 5.5)
- 웨이브 w 의 적 HP = 기본 HP × `hpScale` × `1.07^(w−1)` × 종 보정
- 웨이브 w 마릿수 = `8 + 티어 보너스 + floor(w×1.2)`
- 보스 HP = 기본 × 웨이브 배율 × `(1.1 + n×0.015)` (S50 에서 1.85배)
- 적 처치 골드 = 기본 × `(1 + (n−1)×0.02)` × `(1 + w×0.03)`, 웨이브 클리어 보너스 `20 + w×3 + n×2`

### 레인 (적 동선)

| 종류 | 누가 다니나 | 출처 | 표시 |
|---|---|---|---|
| `ground` 흙길 | 땅 적, 땅 보스 | 배경 아트의 길 (`MAP_LAYOUTS.path`) | 아트 |
| `ground2` 흙길2 | 땅 적 (흙길과 번갈아) | 하드 배경의 두 번째 길 (`MAP_LAYOUTS_HARD.path2`) | 아트 + 코드 포탈 |
| `air` 하늘길 | 공중 적, 공중 보스 | 코드 생성: 포탈→크리스탈 호 (`buildAirLane`) | 구름 점선 |
| `tunnel` 땅굴 | 땅굴 적 | 코드 생성: 흙길 옆 오프셋 (`buildTunnelLane`) | 흙더미 점선 |

- 흙길2가 필요한 티어(4·5)인데 `path2` 가 없으면 **땅굴로 대체**된다. 현재 31~50 은 전부 `path2` 가 있어 실제 두 갈래 흙길로 동작한다.
- 같은 종류 레인이 여럿이면 적을 순번으로 나눠 보낸다 (`laneFor`).
- 첫 레인의 포탈은 아트에 있고, 나머지 레인의 시작점에는 `props/portal.png` 를 코드가 그린다.
- 공중 적 해금 웨이브 3, 땅굴 적 해금 웨이브 5.
- 공중 적의 기믹 = **동선 무시, 포탈→크리스탈 직행**(하늘길). 대공 면역은 없다 — 모든 타워가 공중을 때린다. 인피니티에는 하늘길이 없어 공중 적도 트랙을 똑같이 돈다.
- 스테이지의 누수(적이 크리스탈 도달 → `def.dmg` 만큼 목숨)와 웨이브 클리어(적 전멸)는 그대로다. 무한 순환·필드 한계선은 **인피니티 전용 룰**(`LANES[0].loopAt` 이 있을 때만).

### 석단 (타워 자리)

- 기본 석단 = `MAP_LAYOUTS.spots` (아트의 석단 위).
- 티어 추가 석단 = `MAP_LAYOUTS_HARD.spots2` 가 있으면 그것을 먼저, 부족하면 코드가 흙길 양옆 평지에 생성 (`buildExtraSpots`: 길에서 46px 이상, 다른 석단과 60px 이상, 포탈·크리스탈에서 96px 이상).
- **물 회피**: 게임·에디터가 배경 픽셀을 읽어 파랑 우세(물·바다) 지점을 거른다 (`makeAvoidFromImage`). 온통 파란 맵(얼음호수 등)은 2차 패스로 회피 없이 채운다.
- 코드 생성 석단은 인게임에서 **돌 받침을 코드로 그린다** (`SPOT_BASE` 이후 인덱스).
- 맵이 바뀌어도 타워는 같은 슬롯 인덱스에 남는다.

### 타워 (눈 = 종류, 높을수록 강함)

| 눈 | 이름 | 역할 | 파일 |
|---|---|---|---|
| 1 | 궁수 주사위 | 속사 레이저 | `casual/towers/t1-a.png` |
| 2 | 대포 주사위 | 쌍포 광역 splash 60 | `casual/towers/t2-a.png` |
| 3 | 마법 주사위 | 자수정 마력탄 | `casual/towers/t3-a.png` |
| 4 | 서리 주사위 | 사방 냉기 둔화 | `casual/towers/t4-a.png` |
| 5 | 전격 주사위 | 연쇄 번개 | `casual/towers/t5-a.png` |
| 6 | 폭군 주사위 | 폭발 주사위 투척 | `casual/towers/t6-a.png` |

**모든 타워는 공중 적을 때릴 수 있다** (`TOWER_DEFS[*].canAir = true`, 7~20성 포함). 공중 적의 기믹은 대공 면역이 아니라 '동선을 무시하고 도착점까지 직행'이다.

스킨 a~e: 상점에서 젬 20으로 해금·장착 (`SAVE.equippedSkin`). b~e = 숲/얼음/로열/공허 (2026-09-02 납품).

공격 모션: 배치된 스킨 스프라이트를 그대로 두고 발사 순간 반동(가로 +7%·세로 −9% 눌림, `kick²`로 감쇠)과 원소색 발광을 얹는다 (`paintTowerBody`). `tN-attack-2x2.png` 시트는 스킨과 디자인이 달라 **로드·사용하지 않는다** — 시트로 바꾸면 배치한 타워와 다른 타워가 공격하는 것처럼 보이고, 프레임이 원본 크기(256px)로 그려지는 문제가 있었다.

레벨: 데미지 `[1, 1.6, 2.4]`, 사거리 `+0/+12/+24`, 연사 `1/0.92/0.85`. 앵커: 타워 발밑(받침) = `(t.x, t.y)` = 석단.

### 인피니티 모드 (무한 투기장)

| 항목 | 값 |
|---|---|
| 해금 | **없음.** `infinityUnlocked()` 는 항상 true — 처음 켠 사람도 로비 **맨 위** 두 버튼으로 바로 들어간다. 스테이지 진행과 완전히 분리 |
| 성격 | **풀파워 무한 모드** (랜덤다이스 레퍼런스): 스테이지가 젬으로 타워를 하나씩 해금하며 성장하는 컨텐츠라면, 인피니티는 이미 전부 해금된 상태에서 얼마나 오래 버티느냐를 겨룬다. 저장 데이터의 해금 상태와 무관하게 **6눈 전부**(`unlockedFaces()` 가 인피니티에서 `[1..6]`), 석단 15개 전부, 레인 4개 전부가 1웨이브부터 열린다 |
| 맵 | `cInf` 무한 투기장 — **랜덤다이스식 보드**: 가운데 3×5 석단 보드(15개, 가로 88·세로 72 간격, 중심 512,300)와 보드에 밀착한 둥근 사각형 트랙 **하나**(좌 250·우 774·상 150·하 450, 모서리 52 — 가운데 줄에서 트랙까지 150px). 렌더(`buildRoadLayer`): 바닥(`tiles/arena/floor.jpg` → 없으면 코드: 방사 그라데이션 + 노이즈 얼룩 + 석판 줄눈 + 룬 원 + 비네트) → 보드(`board.png` 패턴 → 없으면 돌 그라데이션 + 노이즈, 옆면·베벨·인셋 그림자·금색 룬 테두리·모서리 룬, 소켓은 `pad.png` → 없으면 오목한 코드 소켓) → 트랙(`drawRoad` 모양 + `road.png` 패턴 → 없으면 코드 석판 줄눈, 경사 연석) → 기둥 4(`prop-2`, 캔버스 좌우 가장자리 x 64/960·y 210/470, 80px)·화로 2(`prop-1`, x 64/960·y 340, 56px, 불꽃은 매 프레임)·잔해(`prop-3`) → 시작/도착(`start.png`/`end.png` 있으면 포탈·크리스탈 대신) → 연석 바깥 어둡게(`dimOutsideTrack`, 36%, 연석+58px 부터 번짐). **장식 규칙**: 장식은 연석에서 ≥100px 떨어진 가장자리에만, 타워(116px)보다 작게(≤80px) — 장식이 타워로 읽히지 않게. 조각 프롬프트 `GROK-BRIEF.md` §F. **시작·도착 지점이 없다**: 적은 왼쪽 화면 밖(−40,300)에서 입구 길(290px)로 들어와 트랙(1,557px)을 시계 반대 방향으로 **영원히 돈다**(경로 끝에 닿으면 `loopAt`=290 으로 되돌아감, `e.laps` 증가). 출구가 없으므로 '도착'으로 목숨이 깎이지 않는다. 대신 **필드 한계선** `INFINITY.fieldCap`=200: 살아있는 적이 200을 넘는 순간 가장 먼저 스폰된 적이 사라지며 목숨 `capDmg`=1 차감(`enforceFieldCap`, 붉은 링 fx). **사라지는 적이 보스면 남은 목숨과 상관없이 즉시 런 종료**(`S.inf.bossLeak`, 결과 창에 표시). 웨이브 칩에 `필드 n/200`(180 이상 붉게). 웨이브 완료 = **스폰 큐 소진**(남은 적 무관) → 골드 지급 → 6초 뒤 자동 다음 웨이브. **보스 웨이브(10의 배수)는 보스를 잡을 때까지 다음 웨이브를 막는다**(메운디 보스 라운드, 제한시간 5분 20초 그대로) — 막지 않으면 보스가 잡몹 속에 묻혀 제한시간으로 죽는 구조였다. 포탈·크리스탈은 그리지 않는다(`noGoal`). 그리기용 폴리라인은 `roads`(입구·닫힌 트랙·출구) — 적 경로는 트랙을 여러 번 겹쳐 돌기 때문에 따로 둔다. 입구·출구 길은 화면 가장자리로 갈수록 어둠에 잠긴다. 배경은 바닥 그림(`casual/maps/map-inf-arena.jpg`, 없으면 코드 바닥 + 돌 단 보드)만 쓰고 트랙·석단·포탈·크리스탈은 코드가 그린다 (`buildArenaLayout`, `buildRoadLayer`, `drawArenaFloor`) |
| 사거리 | 인피니티 보드 보정 `INFINITY.rangeBonus = 160` (`towerRange`) → 가운데 칸(512,300)에서 트랙의 가장 먼 지점(모서리 호 바깥, 284px)까지 가장 짧은 타워(대포 135)도 닿는다. 즉 중앙 타워는 트랙 전체를 때린다 |
| 레인 | **트랙 하나**(≈1,470px). `INFINITY.tier.lanes = ['ground']` 이라 공중·땅굴 적도 `laneFor()` 폴백으로 같은 트랙을 돈다 (랜덤다이스와 같음). **인피니티에서는 어떤 적도 동선을 무시하지 못한다** — 공중 적은 떠서 그려질 뿐 경로는 같고, 모든 타워가 때릴 수 있다. 석단 15 (보드, 코드 받침) |
| 시작 | 골드 400, 목숨 20, 웨이브 상한 없음, 웨이브 간 6초 |
| 적 HP | 기본 × `1.8 × 1.045^(w−1)` × 종 보정 (w30 ≈ 6.4×, w50 ≈ 15.6×, w100 ≈ 142×). 보드 맵·풀파워 기준으로 봇 재보정(§9) |
| 마릿수 | `12 + floor(w×0.6)` (최대 36), 간격 `0.8 − 0.01w` (최소 0.3초) |
| 속도 | 웨이브 40부터 +1%/웨이브 (최대 1.5×) |
| 정예 | 5의 배수 웨이브에 2마리(20부터 3마리): HP×3, 크기×1.2, 골드×3, 금테 표시 |
| 보스 | 10의 배수 웨이브: 100 보스 순환, HP = 기본 × 웨이브 배율 × `(0.7 + 0.012w)` (개별 보정 없음). 20부터 2마리 |
| 골드 | 처치 골드 × `(1 + 0.025w)` |
| SP | 웨이브 클리어 +1, 10의 배수 +3 |
| 종료 | 목숨 0 또는 HUD ≡ 버튼(포기). 완료 웨이브 = `S.wave − 1` 기준으로 기록 |
| 사거리 | 아레나별 보너스 (`map.rangeBonus`): 가로 160 · 세로 280 |
| 젬 | `floor(웨이브/5) × 2` + 신기록 갱신 +10 + 최초 도달 마일스톤(10/25/50/75/100/150/200)당 +15 + 도전 클리어 +80. 인피니티만 해도 상점(스킨 20 · 6눈 90)이 돌아가도록 올렸다 — 첫 런 12~29젬, 6판이면 200젬 대 |
| 저장 | `SAVE.infBest`, `SAVE.infRuns`(최근 5회), `SAVE.infMilestones` |

정의: `content.js` `INFINITY` (`wave(w)` 가 위 수치를 돌려주고 `game.js` `buildInfinityWave` 가 그대로 쓴다).

**보물상자 갓챠 + 다면체 주사위 + 성(★) 타워** — 인피니티의 운 요소. 스타크래프트 유즈맵 **메이플 운빨 디펜스(메운디)** 의 등급·확률·경제를 그대로 옮겼다 (`content.js` `INFINITY.chest`, `game.js` `buyChest`/`rollDie`/`finishSlot`).
- 등급 9개와 확률(원작 그대로, 합 100.099%): 일반 50% → d1(1★ 확정) · 레어 33.1% → d4 · 고대 10.2% → d6 · 유물 5.1% → d8 · 서사 0.8% → d12 · 전설 0.5% → d20 · 에픽 0.2% → d20(14★↑) · 신화 0.08% → d20(18★↑) · 태초 0.019% → 20★ 확정. `chest.min[kind]` 이 최소 눈.
- 경제: 원작 시작 25미네랄·뽑기 10미네랄(2.5회) → 시작 400G·**뽑기 160G 고정**(회차 상승 없음).
- **뽑기 = 즉시 타워**: 인피니티에는 기본 주사위(40G 굴리기·트레이 물리 주사위)가 없다(`canRoll` false, `drawDie` 생략). HUD 왼쪽 `#roll-btn` 이 `🎁 뽑기 160G` 로 바뀌어 `buyChest()` 를 부르고, 등급이 정해지면 **언제나 바로 굴려**(`rollDie`) 손에 타워가 된다. 주머니·보관 체크박스·굴리기/도박 팝오버는 없다 — 뽑았으면 배치가 다음 단계다.
- **뽑기 잠금**: 손에 타워가 있거나 굴리는 중이면 `#roll-btn` 이 비활성(`배치 후 가능`)이라 손이 한 번에 하나만 찬다.
- **보상 대기열** `S.inf.queue`: 보스 보상 주사위는 큐에 쌓이고, 매 프레임 `pumpQueue()` 가 손이 비는 대로 하나씩 자동으로 굴린다. 남은 개수는 뽑기 버튼 아래 `#queue-chip`(`보상 대기 N`).
- **도움말** `#inf-help`(스테이지 위 카드, `?` 버튼·`DKhelp()`): 뽑기 확률표→즉시 타워, 보상 대기, 확률강화, 판매 제한, 파워업, 패배 조건. 첫 인피니티 진입 시 자동으로 한 번 열림(`localStorage.dk_infHelpSeen`).
- 라운드 락은 넣지 않는다. 원작 4.04에서 몰래 넣었다가 확률 조작으로 밝혀진 요소(나무위키 §7.1)라 확률표를 있는 그대로 쓴다.
- **보스 보상 스케줄** `INFINITY.bossReward(w)` (원작 24/37/58/79/90/95/96~100R, 1미네랄 = 16G): 웨이브 10 → 800G+유물(d8) · 20·30 → 800G+서사(d12) · 40 → 1120G+전설(d20) · 50 → 1600G+전설 · 60 → 2400G+전설 · 70 이상 → 1600G+전설+(유물/서사 번갈아). 보스가 n마리인 웨이브는 보스마다 주사위 전부 + 골드 1/n (2026-09-06, 주사위 손실 수정).
- **상성** (`TOWER_DEFS[*].atk`, `INFINITY.sizeMult`): 진동형(궁수 1) 소100/중50/대25 · 폭발형(대포 2·폭군 6·별 7~19★) 소50/중75/대100 · 일반형(마법 3·서리 4·전격 5·태초 20★) 전부 100.
  **보스는 상성을 받지 않는다 — 어떤 공격형이든 1배다** (`game.js damageEnemy` 의 `!e.isBoss` 가드, 검사 `npm run test:boss-matchup`). 순수운빨은 뽑은 눈이 전부인 모드라, 보스 크기와 공격형이 안 맞는다는 이유로 판이 통째로 막히면 운이 아니라 상성 퍼즐이 된다 (100웨이브 보스가 소형이라 폭발형 15★ 3레벨로 15칸을 다 채워도 시간 안에 못 잡았다). 잡몹 상성은 그대로라 배치 판단은 살아 있다. 웨이브 크기는 원작 1~101R 표(`sizeSeq`)를 순환(`sizeOf(w)`), 소 ×0.9 / 대 ×1.15 로 그려진다. 웨이브 시작 텍스트·칩에 소형/중형/대형 표시, 타워 패널에 공격형 표시.
- **방어력** `INFINITY.armor(w) = ⌊max(0, w−20)/4⌋`, 33의 배수 웨이브는 ×8(고방어, 버블피시 255). 피해 = max(피해×0.1, 피해−방어). 에픽(14~17★)은 무시.
- **첫 런 코치** (`game.js` `COACH`, `#coach`): 스테이지 모드를 거치지 않고 들어온 사람에게 조작을 직접 가르친다. 대상 요소 위에 링과 말풍선만 얹고 `pointer-events: none` 이라 진행을 막지 않는다 — HUD 는 `getBoundingClientRect()`, 캔버스 석단·타워는 `canvasToClient()` 로 좌표를 잡는다. 5단계(뽑기 → 석단에 배치 → 웨이브 시작 → 타워 선택 → 파워업)이고 **실제로 그 행동을 해야** 넘어간다. `건너뛰기` 또는 완주 시 `localStorage.dk_coachDone`. 4단계(타워 선택)에서 연 정보 카드가 5단계 대상(파워업 패널)을 덮으므로, 5단계로 넘어갈 때 카드를 자동으로 닫는다. 코치가 끝나면 도움말 카드를 한 번 연다.
- **도움말 자동 오픈**은 카드를 **실제로 닫을 때** `dk_infHelpSeen` 을 저장한다(예전에는 열기 전에 저장해서 한 번 놓치면 영영 안 떴다).
- **파워업 비용은 좁은 화면에서도 보인다**: 겹침 HUD 폭이 760px 미만이면 `narrow` 가 붙는데, 예전에는 그때 `.inf-cost` 를 통째로 `display:none` 으로 숨겨서 파워업이 얼마인지 알 수 없었다(작은 태블릿 가로·화면 분할이 이 구간에 든다). 비용을 숫자(`.inf-cost-n`)와 단위(`.inf-cost-u`)로 나눠, 좁을 때는 단위 `G` 만 접고 숫자는 남긴다. 버튼은 34×44. 360~1440px 47개 크기에서 숨김·넘침 0건.
- **상시 힌트**: 손에 타워가 있는 동안 `#hud-hint` 를 숨기지 않고 "빈 석단을 눌러 타워를 놓으세요"로 바꾼다 — 배치 안내가 `title` 속성뿐이라 터치 기기에서 보이지 않던 문제.
- **로비 동선**: 로비는 **허브**(`#lobby-hub`: 싱글플레이 · 멀티플레이 · 상점 세 버튼 + 젬) 아래 **갈래**가 같은 상자 안에서 펼쳐진다(`lobbyShow(view)`, `LOBBY_VIEW`). 싱글(`#lobby-single`) = 인피니티 도전·무한 + 기록(`#lobby-inf`) + 스테이지 선택 + 진행 줄, 멀티(`#lobby-multi`) = `#mp-block`(이름·방 만들기·코드 참가·빠른 매칭). ← 버튼·Esc·앱 뒤로가기는 갈래 → 허브. 돌아올 때는 온 곳의 갈래를 연다(`gotoLobby('single'|'multi'|'hub')`: 결과 화면·스테이지 선택 뒤로 → 싱글, 대기실 나가기·관전 종료 → 멀티, 상점 뒤로 → 허브). 스테이지 선택 화면의 인피니티 배너는 없앴다.
- **두 갈래 모드** (`INFINITY.modes`, `startInfinity(kind)` → `S.inf.mode`): 나중에 붙일 멀티는 클리어가 있는 쪽, 싱글은 진짜 무한 쪽을 쓴다.
  - **도전** `clear` — **101웨이브 완주 = 클리어**(`clearWave`, 로스터 한 사이클 = 메운디 1~101R). 클리어하면 승리 오버레이로 **런 종료**, 젬 `clearGems` 60 보너스 + `SAVE.infClears` 적립. 웨이브 칩 `∞ 웨이브 12/101`. 로비(싱글 갈래) `#btn-inf-clear`.
  - **무한** `endless` — 클리어 선이 없다. 101을 넘기면 2주기(색상 hue 이동·`N주기` 접두사·탱커 방어 +1)로 계속되고 `SAVE.infBest` 기록만 남는다. 웨이브 칩 `∞ 웨이브 130 (2주기)`. 로비(싱글 갈래) `#btn-infinity`.
- **최종 관문 (도전 모드 전용)** `lateFrom: 60` · `lateExp: 0.9975`: 61웨이브부터 체력 배율에 `0.9975^(w−60)` 이 한 번 더 곱해진다. 밑이 1 미만이라 **감산**이고, 지금은 그 폭이 아주 작다(w101 에서 약 10%). 1~60웨이브는 한 톨도 바뀌지 않는다.
  - 예전 값 `lateFrom: 90` · `lateExp: 1.08` 은 클리어 확률을 소수점대(0.045~0.42%)로 묶으려던 장치였다. 실측은 소수점대가 아니라 **0** 이었다(봇 120런 클리어 0회, 전부 보스 시간초과).
  - 벽을 실제로 연 것은 곡선이 아니라 **보스 상성 면제**다. 같은 곡선(`lateExp 0.99`)에서 클리어율이 2.3% → 14.1% 로 뛰었다. 그래서 감산을 0.9975 까지 되돌렸다.
  - 봇 실측: `lateExp 1.00` → 0.4%(256런) · `0.995` → 4.7%(64런) · `0.99` → 14.1%(64런).
    채택한 **`0.9975`** 는 겹치지 않는 씨앗 1,024런에서 1.37%(14회, 신뢰구간 0.82~2.28%)였고,
    2026-09-11 의 광역 명중 수정(큰 적이 폭발 타워에 면역이던 버그) 뒤 256런에서 **2.0%**(5회, 신뢰구간 0.8~4.5%)다.
    둘 다 목표 1~3% 안이라 곡선은 그대로 둔다. `npm run measure:clear-rate`.
- **보스 주기는 로스터 기준** `isBossWave(w)` / `bossOrdinal(w)`: 로스터가 101웨이브 사이클이라 2주기부터 `w % 10` 과 어긋난다(웨이브 111·121…이 보스). 보스 그림 인덱스·보스 2마리 여부·보스 보상 스케줄·보스 홀드가 모두 순번을 쓴다. (이전에는 `w / 10` 을 그대로 인덱스로 써서 웨이브 111에서 `boss.base` 참조 오류가 났다.)
- **보스 제한시간** 320초(`bossTimeLimit`, 원작 5분 20초). 보스 스폰 시 `S.inf.bossT` 카운트다운(칩에 `보스 m:ss`, 30초 미만 붉게), 0이 되면 남은 목숨과 상관없이 런 종료.
- **4족 뒷다리 부착 위치** (`tools/art-review/directional-101/*-rigs.json` 의 side 뷰 `leftHind`/`rightHind` `socketNormalized`): 뒷다리는 **엉덩이 아래**에 붙어야 한다. 소켓 x 는 몸통 부품의 잘라낸 상자 기준 비율이고, 앞다리는 어깨(0.6~0.8), 뒷다리는 엉덩이(0.11~0.30)에 온다.
  - 2026-09-11 까지 뒷다리 소켓이 몸통 한가운데 쪽으로 0.05~0.14 치우쳐 있어, 네 다리가 전부 가슴·배 밑에 몰리고 엉덩이와 꼬리 쪽 절반에는 다리가 없는 형태로 보였다. 늑대(w017)가 0.44, 도마뱀(w023) 0.39, 악어(w027) 0.35 로 가장 심했다.
  - 고친 값: w011 다람쥐 · w013 여우 · w014 오소리 · w017 늑대 · w018 고슴도치 · w019 곰 · b020-2 · w022 두꺼비 · w023 도마뱀 · w027 악어. `socketRoiNormalized` 도 같은 양만큼 옮겨 소켓을 품게 한다.
  - 다시 만들기: `node tools/build-directional-art.mjs --config=<rigs.json> --out=gen/x` → `node tools/check-directional-art.mjs gen/x --require-ready` → `node tools/promote-directional-art.mjs gen/x`.
- **광역 명중 판정** (`projHit` 의 splash 분기): 명중 지점 `hy` 는 대상의 몸통 중심(발밑 − 크기×0.4)이다. 다른 적과의 거리도 **같은 기준으로 맞춰 빼야** 한다 — `(ep.y − e.def.size×0.4) − hy`.
  - 2026-09-11 까지 이 자리에서 부호를 반대로 써서(`ep.y − hy + size×0.4`) 조준해서 맞힌 대상 자신과의 거리가 0 이 아니라 **0.8×크기** 로 나왔다. 적이 클수록 광역 타워에 면역이 되어, 대형 보스(크기 120 → 거리 96)에게는 **2·6·7~16 눈의 폭발이 통째로 빗나갔다**. 중형은 2·6·7~13, 소형은 2·6·7~11 이 면역이었다.
  - 잡몹은 크기 42~58(거리 34~46)이라 광역 반경 55 이상에 늘 들어와서, "잡몹은 잘 죽는데 보스만 안 죽는" 형태로 나타났다. 10웨이브 보스를 9★·8★ 로 5분 21초 동안 사거리 안 76.8% 에 두고도 체력이 1도 안 깎였다.
  - 검사 `npm run test:splash-hit` — 보스 크기 세 종류 × 광역 타워 8종, 24조합에서 피해가 실제로 들어가는지 본다.
- **타워 옮기기** — 두 가지 방법이 있다. ① **길게 누르기**(`MOVE`, `moveArm`/`moveLift`/`moveTrack`/`moveDrop`): 석단 위 타워를 **380ms** 누르고 있으면 떠올라 손끝을 따라오고, 빈 석단에 놓으면 그리로 옮겨진다. ② **이동 버튼**(`#move-btn`, `movePickStart`/`movePickTap`/`movePickCancel`): 타워를 눌러 정보창을 열고 `이동` 을 누르면 자리 고르기가 되고, 빈 석단을 한 번 탭하면 옮겨진다. 길게 누르기를 발견하지 못하는 경우가 있어 같은 일을 하는 버튼을 뒀다.
  - 고르는 동안: 버튼이 `이동 취소` 로 바뀌고(`.picking`), 놓을 수 있는 빈 석단이 강조되며, 화면 위 배너에 `타워를 놓을 곳을 선택해 주세요` 가 **계속** 뜬다(떴다 사라지는 글자만으로는 놓치기 쉽다). 점유된 칸을 고르면 옮기지 않고 고르기를 유지하고, 빈 곳 밖을 누르거나 버튼을 다시 누르면 취소된다.
  - 빈 석단이 하나도 없으면 버튼이 비활성이다. 비용 없음 — 위치만 바뀌고 눈·레벨·능력치는 그대로다.
  - 떠오르기 전에 화면 기준 14px 넘게 움직이면 길게 누르기로 보지 않는다(긁기·오조작). 손에 주사위를 든 동안에는 배치가 우선이라 시작되지 않는다.
  - 떠 있는 동안에는 **사격하지 않는다**(`towerFire` 의 `t.moving` 가드) — 커서로 적을 쫓는 것을 막는다. 놓으면 0.25초 쉬고 다시 쏜다.
  - 이미 타워가 있는 석단·석단 밖에 놓으면 제자리로 돌아온다. 아레나를 다시 굽거나(`relayoutArena`) 런이 끝나면 들어올린 상태를 정리한다.
  - 그리기: 바닥에 그림자를 남기고 몸체를 22px 띄우며, 깊이 정렬에서 맨 위로 올려 다른 타워·적에 가려지지 않게 한다. 검사 `npm run test:tower-move` (16검사).
- **합체 레벨 점** (`draw` 의 개체 정렬 뒤 후처리): 타워마다 제 몸과 같이 그리면 앞줄 타워가 뒷줄 타워의 점을 가려, 맨 아랫줄 말고는 몇 강인지 보이지 않았다. 정렬이 끝난 뒤 한 번에 그리고 어두운 알약 배경을 깔아 무엇 위에 얹혀도 읽히게 한다.
- **확률강화** (도박, 타워 상태창 `#enhance-btn`) `INFINITY.enhance` / `enhanceDef`·`enhanceTower`: 필드의 타워를 고르고 골드를 걸면 **강화(★+1) · 유지 · 소멸** 셋 중 하나가 나온다. 합체 레벨 `lvl` 은 강화해도 유지되고, 소멸하면 타워가 필드에서 사라진다(선택도 해제). 20★ 은 `최대 등급` 으로 비활성.
  비용 `round10(160 + 90×face)`, 확률 `up = max(0.10, 0.72 − 0.035×face)` · `boom = min(0.45, 0.03 + 0.022×face)` · 나머지가 유지.

  | 성 | 비용 | 강화 | 유지 | 소멸 |
  |---|---|---|---|---|
  | 1눈 | 250G | 69% | 26% | 5% |
  | 6눈 | 700G | 51% | 33% | 16% |
  | 12★ | 1,240G | 30% | 41% | 29% |
  | 19★ | 1,870G | 10% | 45% | 45% |
- **판매 제한**: 인피니티에서 7★ 이상(전설 이상)은 판매 불가.
- **등급 특전** (`TOWER_DEFS[g].perk`): 14~17★ 에픽 = 방어 무시 + 명중 시 12% 락다운 1.0s(보스 0.4s, `e.stunT`, ⚡ 표시) · 18~19★ 신화 = 공속 ×1.5 · 20★ 태초 = 일반형 + 투사체 명중 시 트랙 위 모든 적에게 피해('태초의 일격'). 7~13★ 전설은 기존 스플래시.
- **다면체 주사위는 진짜 3D**: `POLY` 가 정사면체(d4)·정팔면체(d8)·정십이면체(d12)·정이십면체(d20)를 만들고(정이십면체는 황금비 좌표에서 최소 변 길이로 삼각형 20개, 정십이면체는 그 쌍대), `drawPoly3D` 가 `drawCube` 와 같은 원근·뒷면 컬링·면별 조명으로 그린다. 굴림도 d6 과 같은 자세 행렬(`SLOT.R`)로 회전하고 `slotTargetR()` 로 최종 눈이 정면에 오도록 감속한다. d1 은 구슬(`drawOrb`). `poly-dN.png` 아트가 오면 그림이 우선.
- **아레나가 두 벌이다 — 화면 방향에 맞춰 갈아끼운다** (`content.js` `buildArenaLayout` / `buildArenaLayoutPortrait`, `game.js` `arenaKeyForScreen`·`relayoutArena`):
  - `cInf` **가로** — 트랙 524×300, 보드 **5열×3행**(간격 88/72), 사거리 보너스 160. 짧은 변 576 고정.
  - `cInfP` **세로** — 트랙 540×720, 보드 **3열×5행**(간격 150/118), 사거리 보너스 **280**. 랜덤다이스식 세로 배치. 짧은 변 720 고정. **가로 아레나를 시계 방향 90° 돌린 배치**: 입구가 캔버스 **위쪽 끝 밖(cx, −40)** 에서 위 변 가운데로 들어오고, 트랙은 위 변 왼쪽으로 → 왼쪽 변 아래로 → 바닥 → 오른쪽 변 위로 (가로와 같은 시계 반대 방향).
  - **캔버스는 화면 비율로 만든다** (`game.js` `arenaCanvasForScreen` → `DKCONTENT.layoutArena(m, W, H, inset)`): 가로는 `W = 576 × availW/availH`(720~1600), 세로는 `H = 720 × (availH − HUD)/availW`(1000~1600). 트랙·보드 치수는 고정이고 **중심만 옮겨** 굽는다 — 배경이 100% 코드 생성이라 가능하다. 그래서 아레나가 **레터박스 없이 화면을 다 쓴다**(아이폰 16 Pro Max 가로 956×440 → 1251×576, 세로 440×956 → 720×1270). 입구 길은 캔버스 왼쪽 끝 밖(`x=-40`)에서 시작하므로 캔버스가 넓어지면 같이 길어진다(`loopAt` 이 따라온다).
  - `inset`: 가로에서 위(자원 칩 44px)·아래(겹침 HUD 높이)만큼 트랙을 그 사이 가운데에 세운다. 맵에 `m.inset` 으로 남겨 두고 `fitStage()` 가 비교한다.
  - 리사이즈(주소창 접힘 포함)로 화면 비율이 캔버스와 **2% 이상** 어긋나거나 inset 이 10px 이상 달라지면 `relayoutArena(key, force=true)` 가 같은 방향에서도 다시 굽는다 — 타워는 같은 칸, 적은 같은 진행률. `applyMapLayout()` 이 아레나 맵이면 먼저 `layoutArena` 를 부르고 나서 `setCanvasSize()` 한다. 로드 시에는 기본 크기(1024×576 / 720×1080)로 한 번 굽는다(에디터·테스트용).
  - 화면이 세로(`availW/availH < 0.95`)면 `cInfP`, 아니면 `cInf`. **런 도중 돌려도** `relayoutArena()` 가 타워를 **석단 번호**로, 적을 **경로 진행률**로 옮겨 보존한다(투사체·fx 는 버린다). 15칸이라 석단 인덱스가 그대로 대응한다.
  - 사거리 규칙("중앙 석단은 트랙 전체를 때린다")은 두 아레나 모두 성립: 가로 284 ≤ 335, 세로 426 ≤ 455.
  - 소품(기둥·화로·잔해)과 보드 격자선은 트랙·보드 기준 비례식이라 두 비율에서 모두 맞는다.
- **디자인 시스템** (`style.css` `:root`): 색(`--ink-1/2/3`·`--line-1/2`·`--panel`), 반경(`--r-sm/md/lg/pill`), 입체(`--bevel` 상단 하이라이트 · `--bevel-b` 하단 베벨 · `--edge` 바깥 두께 · `--elev-1/2/3`), 버튼 5종(`--btn-gold/green/magenta/danger/violet` + 각 `-line`). 버튼은 한 벌로 통일돼 hover 는 뜨고 `:active` 는 두께가 사라지며 눌린다. 폰트는 구글 폰트 **Do Hyeon**(제목·숫자)·**Noto Sans KR**(본문)이고, **캔버스 텍스트도 같은 서체**(`uiFont(px)`)를 쓴다 — 못 불러오면 기존 스택으로 폴백.
- **상단 칩**: 금테 + 안쪽 하이라이트 + 아이콘 원형 소켓. 웨이브 칩에도 아이콘(∞)이 생겼고, 목숨 5 이하면 맥동한다. 문구는 스테이지 폭에 따라 3단으로 접힌다(`tiny` 는 `4 · 6/200` 까지) — 좁은 폰에서 두 줄로 넘쳐 아레나를 가리던 문제.
- **메뉴 화면은 스테이지 밖, 화면 전체를 쓴다**: 타이틀 오버레이(`#overlay`)와 로비·스테이지 선택·상점(`.screen`)이 16:9 `#stage` 안에 있어 세로 폰에서 **227px 로 잘렸다**(설명이 반만 보이고 로비 버튼이 하나만 노출). `position: fixed; inset: 0` 로 바꾸고 배경에 키아트(`--keyart-bg`, `game.js` 가 `document.body` 에 지정)를 깔았다 — 검은 여백 대신 게임 아트가 보인다.
- **타워 정보는 HUD 를 덮는 오버레이** (`#info-panel`, `style.css`): 타워를 고르면 `position: absolute; inset: 6px; z-index: 12` 카드가 HUD 전체를 덮는다 — 그 동안 뽑기·파워업 칸은 필요 없다. **절대 위치라 HUD 높이·스테이지 크기에 영향을 주지 않는다**(예전 인라인 팝오버가 `flex-wrap` 을 접어 `fitStage()` 가 스테이지를 줄이던 버그 방지). 넓은 HUD 는 가로 배치, 세로열(`#wrap.side`)·좁은 화면은 세로 배치.
  닫는 길 넷: **판매·확률강화 실행**, 카드 우상단 **`✕`**, **같은 타워 다시 탭**, 캔버스 빈 곳 클릭. 이걸로 가로 세로열의 스크롤(가용 382px 에 내용 547px)이 사라졌다.
- **주사위 포커스** `S.dieFocus`: 손에 든 주사위는 기본이 배치 모드이고, **주사위 칸을 탭하면 포커스가 풀려** 굴리기 전처럼 타워를 고를 수 있다(슬롯이 흐려지고 안내 문구가 바뀐다). 드래그는 포커스와 무관하게 동작하고, 배치에 성공하면 다시 배치 모드로 돌아온다.
  이게 **석단이 가득 찼을 때의 데드락 탈출로**다 — 예전에는 캔버스 클릭이 `if (S.heldDie) { tryPlace(idx); return; }` 로 삼켜져 타워를 고를 수 없었고, 판매·확률강화 패널이 숨겨진 채 주사위를 버릴 방법도 없어 런을 포기하는 수밖에 없었다(7★ 이상은 판매까지 막힌다).
- **락을 애초에 덜 만든다**: `canPlaceAnywhere()`(빈 석단이 있거나 `lvl < MAX_LVL` 타워가 있음)를 기준으로 ① 뽑기 버튼을 막고 라벨을 `석단이 가득 참` 으로, ② `pumpQueue()` 가 보상을 굴리지 않고 큐에 남긴다(칩이 개수를 계속 보여주고, 칸이 나면 그때 굴러간다). 합체 여지가 있으면 지금처럼 뽑을 수 있다.
- **화면 배치는 `fitStage()` 가 정한다** (`game.js`) — 스테이지 크기는 뷰포트 폭이 아니라 **세로 여유**가 정하므로 미디어쿼리만으로는 맞출 수 없다. 배치를 JS 가 고르고 `#wrap` 에 클래스를 붙인다:
  - **over** (인피니티 + 가로 화면): 아레나가 `#wrap` 을 꽉 채우고(테두리·여백 0, `viewport-fit=cover`) **HUD 한 줄(≈67px)이 아레나 아래쪽에 반투명으로 겹친다**(`position: absolute`, 위치·폭은 `fitStage` 가 스테이지에 맞춰 지정, `backdrop-filter`). 순서: 주사위·뽑기 → 타워 상태(flex) → 파워업 6칸(38×48, "파워업" 라벨은 숨김) → 웨이브. 트랙은 `inset` 으로 칩 줄과 HUD 사이 가운데에 서므로 가려지는 것은 빈 바닥뿐. `#log-panel` 은 `--hud-h` 만큼 올린다. 타워 정보 카드는 HUD 아래 모서리에 붙어 **위로 자란다**.
  - **bleed** (인피니티 + 세로 화면): 여백 없이 아레나가 HUD 위 전부를 쓰고, HUD 는 **정확히 두 줄** — 1행 주사위·뽑기 + 타워 상태(flex, 안내 2줄 클램프) + 웨이브, 2행 파워업. (예전엔 `@media ≤720px` 의 `#tower-panel { flex-basis: 100% }` 가 셋째 줄을 만들고 아래 ~50px 이 검게 남았다.) `roomy`·`#rotate-hint` 는 인피니티에서 쓰지 않는다 — 남는 공간은 캔버스가 먹는다. 안전영역(`env(safe-area-inset-*)`)은 HUD 패딩과 칩·미니버튼 오프셋에만 준다.
  - **side** (스테이지 모드, `availW/availH ≥ 1.45 && availH < 620`, 가로 폰): `#wrap` 이 `row`, HUD 가 오른쪽 세로 열(폭 150~240px)이 되고 16:9 스테이지가 **세로를 꽉 쓴다**. 파워업은 3×2 그리드, `#hud-break` 는 숨긴다.
  - **stacked** (스테이지 모드, 세로 폰·데스크톱): 스테이지 위, HUD 아래. 세로에서 스테이지가 폭에 막혀 남는 높이는 `#hud.roomy` 가 가져가 슬롯·버튼·파워업을 키운다(최대 `ROOMY_MAX` 430px). 세로에서만 `#rotate-hint` 로 가로를 권한다(닫으면 `localStorage.dk_rotateHint`).
  - 칩(`#stats`)·미니버튼(`#mini-top`) 축소는 뷰포트 폭이 아니라 **스테이지 폭** 기준(`#stage.small` < 680px, `#stage.tiny` < 520px) — 가로 폰은 폭이 넓어도 스테이지가 좁다. 칩 줄의 오른쪽 여유는 `--mini-w`(미니 버튼 실제 폭, `fitStage` 가 기록)로 — 멀티는 💬 가 더 붙어 버튼이 4개다.
  - `visualViewport` 의 `resize`/`scroll` 에도 다시 맞춘다(모바일 주소창). 단 **채팅 입력 중 키보드가 올라와** `visualViewport.height < innerHeight×0.8` 이면 아레나를 다시 굽지 않고 `--kb` 만 기록해 `#log-panel` 을 올린다(`chatClose()` 뒤 다시 맞춤).
  - **좁은 가로**(over, 작은 폰 가로): `availW < 760` 이면 `#wrap.narrow`(슬롯 40·파워업 32×42·웨이브 84·타워 패널 60 → 한 줄 최소 ≈ 560px, iPhone SE 가로 667 통과), `< 600` 이면 `#wrap.xnarrow`(HUD 두 줄 허용, 파워업 줄이 아래로).
  - **아레나 클램프**(`arenaCanvasForScreen`): 세로 h `880~2000`(880 = 트랙 ±360 + 여백, 2000 = 폴더블 커버·초장신), 가로 w `680~1600`. **거의 정사각 화면**(폴더블 펼침, 비율 0.8~0.95)은 세로 아레나를 쓰되 HUD 두 줄 위의 상자가 납작하면 **캔버스를 옆으로 넓혀**(w = 880×availW/boxH, ≤1400) 여백을 채운다 — 보드·트랙은 가운데 그대로, 남는 폭은 바닥 장식.
  - **안전영역**(노치·홈바·펀치홀): `style.css :root` 의 `--sa-t/r/b/l`(= `env(safe-area-inset-*)`, 앱은 safe-area 플러그인이 `--safe-area-inset-*` 로 주입) 을 칩·미니버튼·로그·뷰 바·관전 알약·상대 카드·오버레이·메뉴 화면·도움말·설정·`#wrap` 패딩 전부에 준다. `#wrap.bleed` 만 패딩 0(아레나는 노치 밑까지, 칩·HUD 가 비킨다). 테스트는 `documentElement.style` 로 `--sa-*` 를 덮어써 기기를 흉내낸다(`scratchpad/devices-test.js`: 16기기 × 2방향 × 11화면 — 가로 스크롤·HUD 줄 수·안전영역 밖 요소·가림·줄바꿈·레터박스·칩/미니버튼 충돌 자동 검사).
  - 짧은 가로 메뉴 `@media (max-height: 480px)`: `.screen-box` 패딩·h1·큰 버튼 축소, `#mp-note` 숨김, 대기실 채팅 64px.
  - 뷰포트 메타 `user-scalable=no, maximum-scale=1` + `touch-action: manipulation` + `overscroll-behavior: none` + `#wrap { height: 100dvh }`(웹뷰·주소창).
- **오디오** (`game.js` 사운드 블록 + `music.js` `DKBGM`): `audio()` 가 `AudioContext` 와 버스 `MASTER(음소거) ← SFX_BUS · MUSIC_BUS` 를 만들고 `DKBGM.attach(ac, MUSIC_BUS)`. 효과음 `tone/noise` 는 `SFX_BUS` 로. 음량은 `SAVE.audio = { music, sfx, muted }`(`applyAudioSettings`), 설정 모달 `#settings`(로비·대기실 ⚙, 슬라이더 2개 + 음소거, Esc/뒤로가기 체인 최우선), `#mute-btn` 도 저장. **첫 제스처**(`pointerdown/keydown/touchend` once)에서 컨텍스트를 깨우고 BGM 시작, `visibilitychange` 로 suspend/resume.
  BGM 트랙: `bgmFor()` = playing/spectate 이면 보스가 살아 있을 때 `boss` 아니면 `battle`, 그 외 `lobby`. 훅: `showScreen`·`showOverlay`·보스 스폰(`DKBGM.set('boss')`+`duck`)·마지막 보스 사망(`battle`). `music.js` 는 25ms 룩어헤드 스케줄러로 3트랙을 합성(로비 84BPM Dm · 배틀 128BPM Am · 보스 150BPM D프리지안)하고, `audio/bgm-<track>.ogg|mp3` 가 있으면 `fetch`+`decodeAudioData` 루프로 자동 교체(MUSIC-PROMPTS.md). ★15+ 획득은 `SFX.win`+덕킹, ★19~20 은 `SFX.jackpot`.
- **획득 연출 아트** (`acquireFx` → `acquireFxArt` / `acquireFxCode`): `vfx/acquire-burst-2x2.png` 가 로드됐으면(`hasArt`) 그림 경로 — 시트 `acquireBurst`·`confetti`·`chestOpen`, fx kind `ringImg`(그림 링 회전·확대, `lighter`)·`column`(빛기둥)·`sprite`(반짝이 파티클) — 아니면 기존 코드 프리미티브. 콘솔 `DKacquire(20)` 로 미리보기. `body.ui-art`(`ui/frame-panel.png` 로드 시)는 HUD·카드·버튼·칩 바·슬롯을 9-slice `border-image` 와 `ui/icon-*.png` 로 바꾼다(ART-PROMPTS §7).
- **터치 타겟**: 캔버스 내부 좌표와 화면 크기가 다르므로 `stageScale()`·`touchExtra(cssRadius)` 로 판정 반경을 화면 기준으로 고정한다 — 탭 선택 24px, 드래그 배치 50px, 던지기 임계값도 `330 × stageScale()`.
- **도움말 카드** `#inf-help`: `#stage` 밖 body 직속 `position: fixed` 모달. `.help-card` 는 머리말·`<ol>`(스크롤)·`#help-close`(고정) 3단 플렉스라 **어떤 화면 높이에서도 닫기 버튼이 보인다**. 배경 클릭과 `Esc` 로도 닫힌다.
- **하단 HUD 구성** (`index.html` `#hud`, `style.css`): 첫 줄 `#dice-panel`(슬롯 + `#draw-col`: 뽑기 버튼·`#queue-chip`) → `#tower-panel`(`flex: 1`) → `#right-panel`(웨이브 버튼, `margin-left: auto`). 인피니티는 `#hud-break` 뒤 둘째 줄에 `#inf-panel`(파워업 1~6 + `?`).
  `#tower-panel` 은 선택한 타워가 없으면 안내 한 줄(`#hud-hint`), 손에 타워가 있으면 `#held-info`, 타워를 고르면 인라인 상태창 `#info-panel`(아이콘·이름·공격형 배지 / 수치 한 줄 / `판매` · `확률강화` · 확률 문구)을 보여준다. `min-height: 84px` + `#tower-panel > * { max-height: 84px }` 로 **선택 여부와 무관하게 HUD 높이가 고정**된다 — 예전 인라인 캡션·팝오버는 `#hud`(`flex-wrap: wrap`)를 접어 `fitStage()` 가 스테이지를 줄이는 버그가 있었다. ≤720px 에서는 `#tower-panel` 이 `flex-basis: 100%` 로 자기 줄을 쓴다.
- **웨이브 로스터 (웨이브 하나 = 몬스터 한 종류)** `INFINITY.monsterFor(w)` / `buildInfinityRoster`: 101웨이브 한 사이클. 크기 클래스(`sizeSeq`)로 후보(S `size≤42` / M `44~48` / L `≥50`)를 거르고, 이동형은 주기(4의 배수 공중, 7의 배수 땅굴, 나머지 땅), hp 오름차순 풀에서 진행도(w/101) 위치의 아직 안 쓴 종을 뽑는다 → 초반 약한 종, 후반 튼튼한 종, 사이클 안 중복 없음. 탱커(`TANK_IDS`: 전차·골렘·방패·요새·천산갑 등)는 hp ×1.25, 방어 +2(사이클마다 +1). 보스 웨이브(10의 배수)는 **보스만**. 정예(5의 배수)는 같은 종 3마리 HP ×3. 마릿수 = `min(36, 12+0.6w)` × (소 1.2 / 중 1.0 / 대 0.8). 102웨이브부터 사이클 반복: 색상 `hue = cycle×97`, 이름 앞 `N주기`. 손질은 `ROSTER_OVERRIDES[w] = baseId`. 체력 = base.hp × 1.8×1.08^(w−1) × 탱커 배율, 방어 = `armor(w)` + 탱커 보너스.

<details><summary>1사이클 로스터 표 (101웨이브, 생성 결과)</summary>

| 웨이브 | 몬스터 | 이동 | 크기 | 탱커 | 마릿수 | 방어 |
|---|---|---|---|---|---|---|
| 1 | 성냥개비 (hp 18) | 땅 | 소 |  | 14 | 0 |
| 2 | 클립정찰 (hp 18) | 땅 | 소 |  | 16 | 0 |
| 3 | 보르조이 (hp 40) | 땅 | 대 |  | 10 | 0 |
| 4 | 민들레홀씨 (hp 16) | 공중 | 소 |  | 17 | 0 |
| 5 | 달님정찰 (hp 32) | 땅 | 중 |  | 15 | 0 |
| 6 | 에뮤질주 (hp 46) | 땅 | 대 |  | 12 | 0 |
| 7 | 모래벌레 (hp 55) | 땅굴 | 대 |  | 13 | 0 |
| 8 | 칼새전령 (hp 16) | 공중 | 소 |  | 19 | 0 |
| 9 | 라마짐꾼 (hp 54) | 땅 | 대 |  | 14 | 0 |
| 10 | **보스** 꼬마 슬라임왕 왕 | 땅 | 대 | | 보스만 | 0 |
| 11 | 라즈베리 (hp 24) | 땅 | 소 |  | 22 | 0 |
| 12 | 군함조 (hp 30) | 공중 | 대 |  | 15 | 0 |
| 13 | 치와와기사 (hp 26) | 땅 | 소 |  | 23 | 0 |
| 14 | 천산갑 (hp 80) | 땅굴 | 대 | ● | 16 | 2 |
| 15 | 흰동가리선원 (hp 36) | 땅 | 중 |  | 21 | 0 |
| 16 | 케찰 (hp 32) | 공중 | 대 |  | 17 | 0 |
| 17 | 푸들요리사 (hp 36) | 땅 | 중 |  | 22 | 0 |
| 18 | 달걀병정 (hp 28) | 땅 | 소 |  | 26 | 0 |
| 19 | 일각경기사 (hp 58) | 땅 | 대 |  | 18 | 0 |
| 20 | **보스** 숲 주사위용 대공 ×2 | 공중 | 대 | | 보스만 | 0 |
| 21 | 흰개미병정 (hp 26) | 땅굴 | 소 |  | 29 | 0 |
| 22 | 카피바라온천 (hp 60) | 땅 | 대 |  | 20 | 0 |
| 23 | 산토끼정찰 (hp 28) | 땅 | 소 |  | 30 | 0 |
| 24 | 불꽃요정 (hp 18) | 공중 | 소 |  | 31 | 1 |
| 25 | 구아바기사 (hp 38) | 땅 | 중 |  | 27 | 1 |
| 26 | 마시멜로정찰 (hp 28) | 땅 | 소 |  | 32 | 1 |
| 27 | 두리안기사 (hp 64) | 땅 | 대 | ● | 22 | 3 |
| 28 | 조약돌골렘 (hp 80) | 땅굴 | 대 | ● | 22 | 4 |
| 29 | 오릭스기사 (hp 64) | 땅 | 대 |  | 23 | 2 |
| 30 | **보스** 사탕 주사위요리사 폭군 ×2 | 땅 | 소 | | 보스만 | 2 |
| 31 | 새우닌자 (hp 28) | 땅 | 소 |  | 36 | 2 |
| 32 | 연새 (hp 20) | 공중 | 소 |  | 37 | 3 |
| 33 | 별정찰 (hp 28) | 땅 | 소 |  | 37 | 24 |
| 34 | 낙타짐꾼 (hp 70) | 땅 | 대 |  | 26 | 3 |
| 35 | 소기사 (hp 70) | 땅 | 대 |  | 26 | 3 |
| 36 | 구름양 (hp 40) | 공중 | 대 |  | 26 | 4 |
| 37 | 눈사람정찰 (hp 40) | 땅 | 중 |  | 34 | 4 |
| 38 | 대추야자 (hp 30) | 땅 | 소 |  | 41 | 4 |
| 39 | 바셋하운드 (hp 42) | 땅 | 중 |  | 35 | 4 |
| 40 | **보스** 해변 호박왕 제왕 ×2 | 땅 | 소 | | 보스만 | 5 |
| 41 | 범고래아기 (hp 70) | 땅 | 대 |  | 29 | 5 |
| 42 | 함정거미 (hp 44) | 땅굴 | 중 |  | 36 | 5 |
| 43 | 그레이트데인 (hp 72) | 땅 | 대 | ● | 29 | 7 |
| 44 | 콘도르정찰 (hp 40) | 공중 | 대 |  | 29 | 6 |
| 45 | 팬더수도승 (hp 72) | 땅 | 대 |  | 29 | 6 |
| 46 | 시츄 (hp 30) | 땅 | 소 |  | 43 | 6 |
| 47 | 토마토포병 (hp 42) | 땅 | 중 |  | 36 | 6 |
| 48 | 백조기사 (hp 40) | 공중 | 대 |  | 29 | 7 |
| 49 | 페럿도둑 (hp 32) | 땅굴 | 소 |  | 43 | 7 |
| 50 | **보스** 눈꽃 주사위예티 군주 ×2 | 땅 | 대 | | 보스만 | 7 |
| 51 | 세인트버나드 (hp 72) | 땅 | 대 | ● | 29 | 9 |
| 52 | 민들레요정 (hp 22) | 공중 | 소 |  | 43 | 8 |
| 53 | 하이에나정찰 (hp 44) | 땅 | 중 |  | 36 | 8 |
| 54 | 레몬닌자 (hp 32) | 땅 | 소 |  | 43 | 8 |
| 55 | 멧돼지기사 (hp 78) | 땅 | 대 |  | 29 | 8 |
| 56 | 매너티선원 (hp 78) | 땅 | 대 | ● | 29 | 11 |
| 57 | 무스레인저 (hp 74) | 땅 | 대 |  | 29 | 9 |
| 58 | 패션프루트 (hp 32) | 땅 | 소 |  | 43 | 9 |
| 59 | 콜리기사 (hp 46) | 땅 | 중 |  | 36 | 9 |
| 60 | **보스** 달빛 사탕골렘 대장 ×2 | 땅 | 대 | | 보스만 | 10 |
| 61 | 코케시 (hp 46) | 땅 | 중 |  | 36 | 10 |
| 62 | 에르민 (hp 32) | 땅 | 소 |  | 43 | 10 |
| 63 | 오크라광부 (hp 34) | 땅굴 | 소 |  | 43 | 10 |
| 64 | 저어새 (hp 30) | 공중 | 중 |  | 36 | 11 |
| 65 | 수박전차 (hp 78) | 땅 | 대 | ● | 29 | 13 |
| 66 | 숫양돌격 (hp 78) | 땅 | 대 |  | 29 | 88 |
| 67 | 밍크도둑 (hp 34) | 땅 | 소 |  | 43 | 11 |
| 68 | 찌르레기 (hp 22) | 공중 | 소 |  | 43 | 12 |
| 69 | 하마선원 (hp 80) | 땅 | 대 | ● | 29 | 14 |
| 70 | **보스** 황금 해적크라켄 수호신 ×2 | 공중 | 소 | | 보스만 | 12 |
| 71 | 악어선원 (hp 82) | 땅 | 대 | ● | 29 | 14 |
| 72 | 검은새 (hp 24) | 공중 | 소 |  | 43 | 13 |
| 73 | 바다코끼리 (hp 84) | 땅 | 대 | ● | 29 | 15 |
| 74 | 프레첼정찰 (hp 34) | 땅 | 소 |  | 43 | 13 |
| 75 | 뱀닌자 (hp 34) | 땅 | 소 |  | 43 | 13 |
| 76 | 물까마귀 (hp 24) | 공중 | 소 |  | 43 | 14 |
| 77 | 도마뱀헬멧 (hp 36) | 땅굴 | 소 |  | 43 | 14 |
| 78 | 거북전차 (hp 85) | 땅 | 대 | ● | 29 | 16 |
| 79 | 허스키썰매 (hp 50) | 땅 | 중 |  | 36 | 14 |
| 80 | **보스** 그림자 태엽올빼미 악당 ×2 | 공중 | 중 | | 보스만 | 15 |
| 81 | 나침반 (hp 36) | 땅 | 소 |  | 43 | 15 |
| 82 | 아키타수호 (hp 52) | 땅 | 중 |  | 36 | 15 |
| 83 | 아기코뿔소 (hp 88) | 땅 | 대 | ● | 29 | 17 |
| 84 | 뉴펀들랜드 (hp 78) | 땅 | 대 | ● | 29 | 18 |
| 85 | 수달창병 (hp 36) | 땅 | 소 |  | 43 | 16 |
| 86 | 원숭이창병 (hp 36) | 땅 | 소 |  | 43 | 16 |
| 87 | 야크전사 (hp 88) | 땅 | 대 | ● | 29 | 18 |
| 88 | 도요새 (hp 24) | 공중 | 소 |  | 43 | 17 |
| 89 | 들소전사 (hp 90) | 땅 | 대 | ● | 29 | 19 |
| 90 | **보스** 불꽃 산호여왕 전설 ×2 | 땅 | 대 | | 보스만 | 17 |
| 91 | 모래고기 (hp 38) | 땅굴 | 소 |  | 43 | 17 |
| 92 | 밀화부리 (hp 24) | 공중 | 소 |  | 43 | 18 |
| 93 | 북극곰아기 (hp 72) | 땅 | 대 |  | 29 | 18 |
| 94 | 듀공기사 (hp 70) | 땅 | 대 | ● | 29 | 20 |
| 95 | 울버린 (hp 58) | 땅 | 중 |  | 36 | 18 |
| 96 | 왕새 (hp 26) | 공중 | 소 |  | 43 | 19 |
| 97 | 아기코끼리 (hp 95) | 땅 | 대 | ● | 29 | 21 |
| 98 | 웜뱃광부 (hp 64) | 땅굴 | 중 |  | 36 | 19 |
| 99 | 도도탐험가 (hp 70) | 땅 | 대 |  | 29 | 152 |
| 100 | **보스** 이슬 유령왕 우두머리 ×2 | 공중 | 소 | | 보스만 | 20 |
| 101 | 호저방패 (hp 70) | 땅 | 중 | ● | 36 | 22 |

</details>

- 위 시스템은 전부 `S.mode === 'infinity'` 분기 안에만 있다. 스테이지 모드는 상성·방어력·특전·확률강화·판매 제한 없음(40G 굴리기와 판매만). 테스트 훅 `DKdamage(e, dmg, src)`, `DKenhance()`, `DKqueue()`, `DKTD`.
- 원작에서 못 가져온 것: 라운드별 몬스터 체력 표(출처 사이트가 이 환경에서 차단). HP 곡선은 봇으로 별도 보정한다(§9).

| 항목 | 값 |
|---|---|
| 스테이지 모드 | 골드 40G 로 기존 d6 굴림 (1~6눈 타워). 변경 없음 |
| 인피니티 뽑기 | HUD `#roll-btn`(`🎁 뽑기 160G`, 고정가) → `buyChest()` 로 등급 결정 → 곧바로 `rollDie()` 로 슬롯에서 굴림 |
| 확률 | 일반 50% · 레어 33.1% · 고대 10.2% · 유물 5.1% · 서사 0.8% · 전설 0.5% · 에픽 0.2% · 신화 0.08% · 태초 0.019% (`INFINITY.chest`) |
| 굴림 | 슬롯에서 다각형 주사위가 돌다 멈춤(`drawPoly3D`, `dice/poly-dN.png` 가 있으면 그 그림 회전) → 나온 숫자 = 타워 성. d1 은 항상 1. 손이 찰 때까지 다음 뽑기는 잠긴다 |
| 성 타워 7~20 | `TOWER_DEFS[7..20]` 를 `STAR_BANDS` 로 생성: 6눈 기준 `dmg 40×1.28^(g−6)`(20성 1,268), `range 175+5(g−6)`, `rate 1.25×0.97^(g−6)`, `splash 55+4(g−6)`, 대공 가능, 폭발 주사위 투척. 밴드: 7~10 별빛 첨탑(하늘색) · 11~14 성운 요새(보라) · 15~18 천공 옥좌(금) · 19~20 차원 군주(무지개). 그림 `casual/towers/star-NN.png` 가 아직 없어 **`buildStarSprite(g, skin)` 이 코드로 구워 쓴다**(성마다 1회 캐시, PNG 가 들어오면 `buildTowerSprites` 가 그쪽을 우선): 6눈 장착 스킨을 밴드색으로 물들이고(`globalCompositeOperation:'color'` — 음영 유지) 받침 룬 링·떠 있는 주사위·밴드 장식(7~10 크리스탈 첨탑 / 11~14 궤도 링 등급마다 +1 / 15~18 날개+후광 / 19~20 공허 오벨리스크+무지개 균열)을 얹는다. `★g` 배지와 펄스 링은 그대로. 같은 성끼리 합체 Lv 업, SP 강화는 1~6눈만 |
| 20성 확률 | 태초 0.019% 확정 + 전설·신화 뽑기에서 20이 나오는 경우 |
| 당첨 연출 | 7 이상: 화면 흔들림 + 링 + "★n 등장!" 텍스트, 15 이상 팡파르 |
| ★ 공격 연출 | 투사체에 `star`·`color`(밴드색)를 실어 크기 `26 + (g−6)×1.1`·밴드색 글로우·3칸 잔상. 발사 때 밴드색 머즐 플래시 + 링, 명중은 `starImpact()` 가 밴드색 충격파 + 파편(성에 비례). 특전별로 epic 흰 파쇄 링 · myth 느린 두 번째 링 · primal 흰 대형 링('태초의 일격'). 배치 마법진 눈금도 성마다 다르다 |

에셋 프롬프트: `GROK-BRIEF.md` §E (성 타워 14 + 다면체 주사위 5 + 상자 1).

**눈별 파워업 (골드)** — 랜덤다이스 인게임 '파워 업' 이식. `content.js` `DICE_POWER`, HUD 파워업 패널 또는 단축키 1~6. 웨이브 SP 는 없어졌다(`spPerWave` 삭제):

| 항목 | 값 |
|---|---|
| 최대 Lv | 10 |
| 비용 | **골드** `150 + 150·Lv` (Lv1 150G … Lv10 1,500G, 눈당 총 8,250G) — 강화할수록 비싸진다 |
| 피해 | `×(1 + 0.15Lv)` (최대 2.5×) |
| 사거리 | `+3/Lv` |
| 특수 (3Lv마다 1단계) | 1눈 연사 −6% · 2눈 광역 +8 · 3눈 피해 +10% · 4눈 둔화 +4% · 5눈 연쇄 +1 · 6눈 광역 +6 & 피해 +8% |

강화는 해당 눈의 모든 타워에 즉시 적용되며 스테이지 모드에서는 항상 Lv0 이다 (`powerLv`). **7★ 이상 히든 타워는 6눈(폭군) 파워업을 따른다**(폭발 주사위 계열). 합체 상한은 Lv3 그대로. 결과 창의 '파워업에 쓴 골드' = `S.inf.spent`.

### 조작 (두 갈래 — 합치지 말 것)

1. **필드 플릭** — 맵 왼쪽 아래 3D 주사위를 잡고 던진다. 윗면이 결과. 적에게 세게 맞히면 속도 비례 피해.
2. **버튼 / R** — HUD `?` 박스 안에서만 회전 후 즉시 획득.

획득한 주사위는 **드래그해서 석단에 놓으면** 해당 눈 타워로 변신. 같은 눈 타워 위에 올리면 합체 하이라이트.

### 연출 (P3 완료)

- 피격 플래시: `e.flashT`, 흰 실루엣 오버레이 (`flashCanvas`, 프레임별 캐시)
- 사망: 잔상이 떠오르며 사라짐 + 먼지 (`spawnDeath`, `S.corpses`)
- 배치/합체 마법진: `fx.kind = 'circle'` (눈 수만큼 룬 눈금, 합체는 금색 6각 별 + 불꽃)
- 보스 등장: 포탈 폭발·마법진·화면 흔들림·**BOSS 등장! 배너**·포효, 1.25초 스케일업(오버슈트) 후 이동 (`BOSS_ENTRANCE`)
- 보스 걷기: 쿵쿵 스쿼시 + 발디딤 먼지·흔들림·스톰프 음. 걷기 시트(`bossBases[].walk`)가 있으면 재생.

### 적 이동

- `ground` 흙길 / 흙길2
- `air` 하늘길 (y −42, 그림자)
- `burrow` 땅굴, 잠수 타이머, 숨으면 타겟 불가

걷기 시트 36종 (slime~meerkat) + 보스 1~10 완료. 파일이 없으면 정지컷 폴백 (`loadImage` 의 `missing` 표식).

---

## 4. 맵 레이아웃 (그리드 + 테마 타일)

**2026-09-02 전면 변경.** 배경 그림에 길·석단 좌표를 맞추던 방식(`MAP_LAYOUTS`, `MAP_LAYOUTS_HARD`, 좌표 에디터)은 폐기했다. 그림과 코드가 미묘하게 어긋나 품질을 떨어뜨렸기 때문이다. 이제 **코드가 격자 위에 맵을 설계하고, 테마 타일을 그 위에 입힌다.** 어긋남이 구조적으로 없다.

`content.js`:
- 격자 16×9 칸, 칸 64px (`TILE`, `GW`, `GH`). 픽셀 좌표 = 칸 중심.
- `TEMPLATES_SINGLE[6]` (스테이지 1~30, 한 갈래) / `TEMPLATES_DUAL[6]` (31~50, 두 갈래). 9줄 × 16글자 ASCII:
  `.` 평지 `#` 흙길 `S` 시작 `E` 도착 `2` 두 번째 길 시작 `=` 두 번째 길(티어 4·5 만) `~` 물 `T` 큰 소품 `x` 석단·소품 금지 `o` 석단 강제(선택).
  길 규칙: 흙길은 4방향으로 이어진 한 줄(옆 줄과 한 칸 띄움), E 위 칸은 비움(성 그림), 두 번째 길은 흙길 한 칸에만 닿음.

### 맵 설계 지침 (2026-09-02, 플레이 피드백 반영)

문제였던 것: 석단이 길과 무관하게 흩어져 있고, 나무가 길을 가렸다. 아래 규칙을 코드가 강제한다.

1. **석단은 길 커버리지로 고른다.** `buildGridLayout` 이 평지 칸마다 점수 = (반경 2.4칸 안의 길 칸 수) + 1.5×(상하좌우 길 이웃) + 0.5×(대각 길 이웃) 을 매기고 높은 순으로 뽑는다. 꺾이는 **안쪽 칸, 길이 두세 면으로 감싸는 칸**이 자연히 1순위. 길에 붙지 않은 칸(이웃 8칸에 길이 없음)은 후보에서 제외 → "길에서 떨어진 석단" 없음.
2. **석단끼리 붙이지 않는다** (체비쇼프 거리 2 이상) → 사거리가 겹치지 않고 길 전체에 퍼진다. 기본 7개 + 티어 추가 2·4·6·8개는 같은 순위 목록의 앞에서부터.
3. **길은 자주 꺾는다.** 긴 직선은 안쪽 칸이 없어 석단 가치가 낮다. 템플릿은 S자·U자·지그재그로 짜서 후보가 티어 5 기준 15개 이상 나오게 한다 (에디터가 검사).
4. **소품은 자연스럽게 흩되 길·석단·타워·포탈·성을 가리지 않는다.** 칸 단위 금지(→ 구석에만 몰리고 가운데가 빔)를 버리고 픽셀 단위로 판정한다 (`buildTileLayer` 5단계):
   - **금지 마스크** (`buildForbidMask`): 길(폭 40 + 여백 9)·석단 타원(42×24) + 그 위 타워 그림 자리(80×108)·포탈/성 그림·HUD 칩/버튼 자리·물 을 오프스크린에 칠해 1비트 마스크로.
   - **후보점**: 브리드슨 포아송 디스크 샘플링(최소 거리 38px, 블루 노이즈) — 맵 전체에 고르게 뿌려 가운데도 채워진다.
   - **무리**: 5×4 저주파 값 노이즈로 밀도 필드를 만들어 높은 곳은 나무 위주 숲 덤불, 낮은 곳은 트인 풀밭(듬성듬성, 꽃·바위 위주).
   - **가림 검사**: 후보마다 종류·크기(±15%)를 정해 스프라이트 사각형(바닥 기준, 위로 h)을 6px 간격으로 마스크와 대조. 하나라도 닿으면 버리고, 큰 나무가 안 들어가면 작은 소품으로 재시도. 그래서 길 **위쪽(뒤)** 에는 큰 나무가 서고 길 **아래쪽(앞)** 에는 캐노피가 길을 덮으므로 자동으로 안 선다.
   - 소품끼리는 면적 30% 까지 겹쳐 무리 느낌을 내고, 최대 90개, y 정렬로 굽는다. 템플릿의 `T` 칸은 큰 나무를 먼저 고정.
5. **HUD 가 가리는 곳에는 석단을 두지 않는다.** 맨 윗줄 전체, 둘째 줄의 왼쪽 7칸(골드·목숨·웨이브 칩), 포탈·성 주변 1칸.
6. 템플릿의 `o` 는 강제 석단(디자이너가 꼭 두고 싶은 자리), `x` 는 금지 칸. 보통은 둘 다 안 써도 된다.
- `templateForStage(n)`: 6개씩 돌려 쓰고 두 바퀴째는 좌우 반전 → 1~30 은 12가지, 31~50 은 12가지 배치.
- `buildGridLayout(rows, mirror)` → `{ grid, path, path2, spots, spots2, padCells, water, props, start, end, start2, …Cell }`. 꺾이는 칸은 모서리를 36% 깎아 곡선처럼 돈다. `spots`(기본 7)·`spots2`(추가 후보) 는 위 설계 지침 1·2 의 점수 순.
- `THEMES[6]`: `plains` 평원(1~8) · `forest` 숲(9~16) · `lake` 호수(17~25) · `darkforest` 어두운 숲(26~33) · `castle` 성(34~42) · `hell` 지옥(43~50). 코드 폴백용 색(바닥·길·물·소품·바위·발광)을 갖는다. `themeForStage(n)`.
- `maps[]` 는 `g1`~`g50` (`tiled: true`, `theme`, `layout`, `path`, `path2`, `spots`, `spots2`, `center`=E, `portals`) + `cInf` 아레나. `buildLayout(map, tier)` 은 그대로: 하늘길·땅굴은 코드 생성, 추가 석단은 `spots2` 앞에서 티어 수만큼.
- `buildArenaLayout()` — 무한 투기장 전용: 입구 길 + 보드 밀착 둥근 사각형 트랙(모서리 호 10° 샘플) 한 바퀴를 이어 붙인 경로 + `loopAt`(순환 시작 거리) + 3×5 보드 석단 15개 + `roads`(그리기용: 입구·닫힌 트랙) + `board`/`track`(`mid` 포함) 사각형 + `noGoal`. (`buildLayout` 은 `map.airPts` 가 있으면 하늘길로 우선 쓰는 분기를 갖고 있지만 현재 아레나는 주지 않는다.)

`game.js`:
- `buildTileLayer(m)` 가 스테이지 시작 때 오프스크린에 한 번 굽는다. 순서: 바닥(`floor.jpg` 없으면 그라데이션+풀결) → 물(모양은 코드 둥근 칸, 표면은 `water.png` 256px 반복 패턴, 없으면 테마색) → 도로(모양·폭·코너·합류는 `drawRoad` 브러시, 본체 40px 를 `road.png` 160px 반복 패턴으로 채움; `road.png` 가 없고 옛 `road-straight.png` 가 있으면 띠 가운데를 잘라 질감으로; 둘 다 없으면 테마색) → 석단(`pad.png` 없으면 코드 받침) → 소품(씨앗 고정 배치, 빈 평지의 24%, S·E·2 주변 제외; `prop-1~6` 없으면 코드 나무·바위·덤불·꽃·비석) → 시작(`start.png` 없으면 포탈 애니) → 도착(`end.png` 없으면 `props/crystal.png`).
- 도로를 타일(직선·코너·T·십자)로 받지 않는 이유: 생성 모델이 폭·진입 위치를 못 지켜 장마다 달라진다(2026-09-02 평원 납품에서 확인). 질감만 받으면 어긋남이 0.
- 타일 키 `tl_<theme>_<name>`, 경로 `casual/tiles/<theme>/<name>.png` (floor 만 jpg). 바닥·도로·물 질감은 raw 이미지, 나머지 오브젝트는 `processSprite`(회색 배경 제거·크롭).
- `ARENA` = `{ center, portals, tiled, theme, hasStart, hasEnd }`. 도착 타일이 있으면 크리스탈 대신 맥동 링만, 시작 타일이 있으면 포탈 그림 대신 발광만 그린다.

`editor.html` (그리드 에디터):
- 템플릿/스테이지 선택, 글자 붓으로 칸 칠하기(우클릭 = 평지), 좌우 반전 미리보기, 티어 미리보기(하늘길·땅굴·자동 석단을 번호와 함께 실제 계산으로), 규칙 검증(이웃 수·연결·E 위 칸·석단 후보 수), `content.js` 스니펫 복사.

캔버스 1024×576. 옛 배경 JPG(`casual/maps/map-NN-*.jpg`)는 로드하지 않고 배포에서도 뺀다(`.assetsignore`). 지워도 된다.

---

## 5. 아트 현황 (디스크 = 카탈로그)

| 항목 | 목표 | 파일 | 상태 |
|---|---|---|---|
| ~~배경~~ | ~~50~~ | `casual/maps/*.jpg` | **폐기** (그리드 + 타일로 대체) |
| 인피니티 아레나 조각 | 9 | `casual/tiles/arena/` | 완료 (납품 2026-09-03, main 37323f2). 기둥 80·화로 56·잔해 48px, 가장자리 배치 + 연석 밖 어둡게 |
| **테마 타일셋** | 6테마 × 12 = 72 | `casual/tiles/<theme>/` | 평원 14/12 납품(`road.png` 대기, 옛 도로 타일 4장 미사용). 나머지 5테마 → `GROK-BRIEF.md`. 없으면 코드 폴백 |
| 몬스터 고유 | 500 | `casual/enemies/*.png` | 완료 |
| 적 걷기 시트 | 순차 | `*-walk-2x2.png` | 36 완료 (1~36). 37~ 은 `NEXT_WALK` 에 id 추가 + ART-PROMPTS §2 |
| 보스 고유 | 100 | `casual/bosses/*.png` | 완료 |
| 보스 걷기 시트 | 순차 | `casual/bosses/*-walk-2x2.png` | 1~10 완료. 11~ 은 `BOSS_WALK_COUNT` 올리기 |
| 타워 인게임 | 6 | `t1-a`~`t6-a` | 완료 (2D 아이소) |
| 타워 스킨 b~e | 24 | `tN-b`~`tN-e` | 완료 (숲/얼음/로열/공허, 납품 2026-09-02) |
| 타워 공격 시트 | 6 | `tN-attack-2x2.png` | 보관(미사용, 스킨과 불일치) |
| 공격 VFX | 눈별 | `vfx/` | 완료 |
| 포탈 (추가 레인) | 1 | `props/portal.png` | 완료 (코드가 그림) |
| 인피니티 아레나 바닥 | 1 | `casual/maps/map-inf-arena.jpg` | **생성 대기** (길·석단·포탈·건물 없는 바닥만) → ART-PROMPTS §5. 없으면 코드 바닥 |

스타일: Kingdom Rush + Random Dice. 캐주얼 치비, 두꺼운 외곽선, 아이소 3/4. 인게임 타워 박스 핏 70×96.

---

## 6. 폴더 지도

```
dicekeep-art/
  index.html          엔트리 (?v= 캐시버스트, <!-- APP --> 자리에 앱 빌드가 app.js 를 끼운다)
  game.js             게임 루프, 레인 이동, 전투, 연출, 입력, 로비/상점, 로그·채팅, 오디오 버스·설정, DKAPP(뒤로가기)
  music.js            BGM 합성 3트랙 + audio/ 파일 대체 (window.DKBGM)
  app.js              앱(Capacitor) 전용 다리 — 빌드 때 Capacitor UMD 와 합쳐 www/app.js 로 (웹 배포 제외)
  fonts/              자체 호스팅 글꼴 (Do Hyeon · Noto Sans KR, tools/copy-fonts.mjs 가 만든다)
  privacy.html        개인정보처리방침 (스토어 등록용 URL)
  package.json · capacitor.config.json · tools/ · resources/ · android/ · ios/   앱 패키징 (§6.6, 웹 배포 제외)
  STORE.md · MUSIC-PROMPTS.md   스토어 등록 자료 · BGM 생성 프롬프트
  net.js              멀티 클라이언트 DKNET v3 (WebSocket 하나: 방 만들기/참가/재접속 · 시각 동기 · 요약 송신). 서버 주소가 없으면 offline
  net/                멀티 서버 — 별도 Cloudflare Worker `dicekeep-net` (Room Durable Object: 방·시계·시드·중계). 정적 배포에서 제외(.assetsignore)
    wrangler.jsonc    main src/index.js · DO 바인딩 ROOM · migrations v1
    src/index.js      Origin 검사 · /ws/new · /ws/room/:code · /health · IP 속도 제한
    src/room.js       Room DO 어댑터 (Hibernation WebSocket API ↔ room-core)
    src/room-core.js  순수 상태 머신 (규칙 전부, Node 테스트 대상)
    src/timing.js · proto.js · codes.js · ratelimit.js
    test/             node --test 단위 · dev-server.mjs(테스트 더블) · ws-smoke.mjs(통합)
  content.js          DKCONTENT (테마/템플릿/그리드 레이아웃/티어/적/보스/스킨/스테이지)
  editor.html/js      맵 그리드 에디터 (ASCII 템플릿 칠하기, 티어 미리보기, 스니펫 복사)
  style.css
  serve.py / start.bat
  GAME-SPEC.md        이 문서
  ART-PROMPTS.md      다음 배치 생성 프롬프트 (하드 배경 / 걷기 시트 / 스킨)
  ART-PIPELINE.md     배치 트래커
  GROK-HANDOFF.md     초기 인수 브리프 (P0~P2 프롬프트 보관)
  ASSET-MANIFEST.md   파일 목록
  casual/
    tiles/<theme>/    테마 조각 (floor, road·water 질감, pad, start, end, prop-1~6)
    maps/             (폐기) 옛 배경 JPG, 아레나 바닥(예정)
    towers/           tN-a~e + tN-attack-2x2
    enemies/          500 대기 + 걷기 시트
    bosses/           100 + 걷기 시트 10
  dice/               HUD·3D 큐브 텍스처 dice-1~6
  towers/             die-1~6 (tN-a 복사본) + 구 archer/cannon/…
  enemies/            구 mite/runner/husk/boss + walk
  vfx/                레이저·포구·폭발·번개·주사위폭탄
  map/battlefield.jpg 구 전장
  props/              portal(추가 레인 포탈로 사용), crystal
  ui/                 gold, heart, title-keyart-p(세로·돌 제목 포함)·title-keyart-l(가로·윗부분), icon-*
```

로드하지 않지만 삭제하지 말 것: `towers/archer.png` 등 옛 5종, `enemies/*` 구 정지컷, `props/crystal.png`.

---

## 6.5 멀티 — 인피니티 · 함께 (`net/` 서버 · `net.js` DKNET v3 · `game.js` 멀티 블록)

**설계 원칙: 서버는 방·시드·중계·순위만 맡고, 시뮬레이션(웨이브·배속 포함)은 각 클라이언트가 자기 보드만 자기 속도로 돌린다.**
게임 방식은 **각자 보드 · 각자 속도**(2026-09-06, v3): 시작 신호(`start{seed,t0,timing}`)만 같고, 그 뒤는 각자 싱글과 똑같이 진행한다 —
막간 6초 자동, 웨이브 버튼으로 앞당기기, 배속 x1/x2/x3, 보스 320초 로컬 타이머. 아무도 누구를 기다리지 않는다.
상호 간섭은 없다. 먼저 죽으면 관전, 101웨이브 완주 = 클리어(도전 규칙 그대로 — 목숨 20 · 한계선 200 · 보스 320초).
순위는 **완주는 빠른 순(`clearAt`), 탈락은 웨이브 순**. 아레나가 기기(가로 cInf / 세로 cInfP)마다 달라도 각자 보드라 문제 없다.
(v2 '각자 보드 · 같은 웨이브 · 서버 시계 · 보스 홀드' 는 폐기 — 보스 대기가 답답하고 배속을 못 쓰는 문제.)

```
[브라우저 A] game.js ── DKNET(net.js, WebSocket 1개) ──wss──┐
[브라우저 B·C·D] …                                        ├─ dicekeep-net (별도 Worker, 루트 net/)
                                                           │   ├ src/index.js  Origin 검사 · IP 속도 · 코드 검사 · /ws/new /ws/room/:code /ws/quick /health
                                                           │   ├ Room DO      방마다 1개 · 명단 · 시드 · 순위 · sum/chat/log 중계 · watch(필드 보기) 라우팅
                                                           │   └ Lobby DO     빠른 매칭 대기열 1개 · 4명 즉시 / 2명 이상 10초 → Room /claim 예약 · 시간당 방 생성 상한
[정적 Worker dicekeep] index.html/game.js/net.js ──────────┘  (루트 wrangler.jsonc 그대로 · 브랜치 프리뷰 유지)
```

DO 가 있는 Worker 는 브랜치 프리뷰 URL 이 생기지 않으므로 서버는 **별도 Worker** 다(§9 배포 절차는 README).

### 접속 · 인증
- 클라 `DKNET.CFG.url` 은 경로 없는 원점. `?net=off` · `?net=ws://…`(localStorage `dk_net` 저장, `?net=clear` 삭제) · localhost → `ws://localhost:8787` · `dicekeep.<계정>.workers.dev`(프리뷰 `xxx-dicekeep.…` 포함) → `wss://dicekeep-net.<계정>.workers.dev`. 없으면 로비 블록이 비활성.
- 방 만들기 `/ws/new`, 참가·재접속 `/ws/room/{CODE}`, 빠른 매칭 `/ws/quick`(`op:'quick'`). **인증은 URL 이 아니라 첫 프레임 `hello`** `{v:3, ver, op, pid, key, name}` (URL·로그에 좌석 토큰이 남지 않게). `pid`(8자)·`key`(32 hex) 는 탭 단위 sessionStorage → 새로고침 = 같은 좌석, 다른 탭 = 다른 플레이어.
- 방 코드: `ABCDEFGHJKMNPQRSTUVWXYZ23456789` 6자리. 거절은 항상 accept 뒤 `err`+close(4404 없는 방 · 4409 full/started · 4410 expired · 4426 version · 4403 bad-key · 4429 rate · 4400 bad-request · 4001 replaced · 4000 leave).
- 방장 = 만든 사람(이탈 시 가장 먼저 들어온 접속자에게 위임). **접속 2명 이상이면 시작**(준비 체크 없음). 방 안 게임 버전(`ver` = index.html `?v=`)이 다르면 `version` 거절.
- **빠른 매칭**: `/ws/quick` → Lobby DO 대기열(같은 `ver` 끼리) → `queued{n,eta}` 방송(변화마다·5초) → **4명이면 즉시, 2명 이상이고 가장 오래 기다린 사람이 10초를 넘기면** 묶어 Room `/claim`(좌석 예약 30초) → 각자 `matched{code}` → 대기열 소켓을 버리고 `/ws/room/{code}` 로 join(예약된 pid·key 는 통과) → 예약 인원 전원 접속 또는 30초 경과·2명 이상이면 **자동 start**(방장 없음), 1명이면 방 폐기(`expired`). 클라 `DKNET.quick(name)` → 상태 `queue` → `queued`/`matched` 이벤트, 나가기 = 취소.

### 프로토콜 (JSON 1개 = 프레임 1개, 봉투 없음, 서버→클라는 `at` 서버 ms 포함, 모든 시각은 서버 ms epoch)
| 방향 | `t` | 필드 |
|---|---|---|
| →서버 | `hello` `start` `leave` `time{c}` | 위 참조 · `time` 은 시각 동기 왕복 |
| →서버 | `sum` | `w dw l g k f sp hid b o tw [ll en]` — 현재 웨이브·마지막 완료 웨이브·목숨·골드·처치·필드 수·배속(1~3)·백그라운드·보스 HP 비율·보드 방향(l/p)·타워 `[[spot,face,lvl]…]≤15`. **벽시계 setInterval 2초**(rAF 밖이라 백그라운드에서도 `hid:1` 로 감). 누가 내 필드를 보고 있으면(`watched{n>0}`) **1초 주기**에 `ll`(내 레인 길이)·`en`(적 스트림 `"i,d,h;…"` — i = bases 인덱스(보스 1000+bossBases), d = dist 정수, h = 체력 0~9, ≤200마리·≤3,000자) 를 싣는다. 서버는 `en` 을 보는 사람에게만 중계(다른 멤버에게는 떼고 1.5초 간격) |
| →서버 | `watch{pid\|null}` | 상대 필드 보기 시작/끝. 대상에게 `watched{n}` |
| →서버 | `done{w}` `dead{w,k,r}` `clear{w,k}` | 완료 블록·`endInfinity`·`checkInfClear` 훅. `r ∈ lives|bossLeak|bossTimeout|quit|reload|afk` |
| →서버 | `chat{text}` `log{text,kind}` | 120자 · chat 1/s · log 2/s |
| →클라 | `welcome{pid,code,resumed,now,room}` `room{…players[],game{t0,timing,seed}}` `player{pid,+변경}` | 명단·재접속 스냅샷 |
| →클라 | `start{seed,t0,timing{prep,bossLimit,clearWave}}` | 시작 신호. `t0 = now + prep(20초)`. 그 뒤 서버에 웨이브 시계는 없다 |
| →클라 | `sum{pid,…}` `chat{pid,name,text}` `log{pid,name,text,kind}` `watched{n}` `time{c,s}` `end{reason,ranking[{pid,name,rank,status,wave,kills,clearAt}],seed}` `err{code,msg}` | 중계·결과 |
| →클라(대기열) | `queued{n,eta}` `matched{code}` | 빠른 매칭 |

`net.js` 는 서버 `t` 를 허용목록으로만 같은 이름의 버스 이벤트로 발화하고, 내부 이벤트는 `net:` 접두(`net:state` `net:reconnecting` `net:offset` `net:closed`) — 이름 충돌이 없다.

### 개별 진행 (서버 `room-core.js` · 클라 싱글 경로 그대로)
| # | 규칙 |
|---|---|
| P0 | 방장 `start`(빠른 매칭 방은 자동) → `t0 = now + PREP(20초)`, 전원 `startInfinity('clear', net)` 로 보드를 열고 `S.autoT = (t0 − serverNow)/1000` — 준비 시간엔 스폰 없음(뽑기·배치 가능). 배속은 전원 x1 로 출발 |
| P1 | `t0` 에 첫 웨이브가 **자동으로** 열린다(웨이브 버튼으로 앞당길 수 있다). 그 뒤는 싱글과 동일: 막간 6초 자동·버튼 조기 시작·`#speed-btn` x1→x2→x3 순환·보스 웨이브는 로컬 320초(`timing.bossLimit`)·`infBossHold` 는 웨이브 번호 기준 |
| P2 | 서버는 `sum`(진행 보고)·`done{w}`(통계)·`dead`/`clear` 만 받는다. 검증은 `w ≤ clearWave` 정도. **홀드·스케줄·따라잡기 없음** — 백그라운드에 가면 그 사람 게임만 멈춘다(rAF) |
| P3 | 순위 `ranking`: `cleared` 는 `clearAt` 오름차순(먼저 완주 = 위, 동시는 pid 순) → `dead`/`lost`/`left` 는 `deathWave` 내림차순 → `kills` 내림차순. 공동 없음 |
| P4 | 종료: alive 0 → `cleared`(완주자 있음) / `all-dead` · 전원 left → `empty` · `t0 + GAME_CAP(100분)` → 남은 alive 는 `lost` → `timeout` |
| P5 | 클라 `frame()` 은 단일 경로 `for (i < S.speed) update(dt)` + `mpTick()`(0.5초: 카드 배지·웨이브 버튼). 상대 카드에 `x2`/`x3` 배지(`sum.sp`) |

### 사망 · 관전 · 결과 · 저장
- `endInfinity(won)` 는 멀티면 `netRunOver(won)`: **젬·최고 기록·infRuns(`mp:1, code`) 를 그 즉시 저장**(`settleInfRun`) → `dead`/`clear` 보고 → `S.phase='spectate'`(update 조기 반환으로 보드 정지) → `#spectate` 패널(현재 순위·남은 인원·작게 보기·나가기). 먼저 죽은 사람이 나가도 보상을 잃지 않는다.
- `end` 수신 → 순위표 오버레이(`table.rank`). 완주자는 `완주 · 38분 12초`(`clearAt − t0`), 탈락은 `웨이브 N`. `SAVE.mp {games, wins, best}` 적립, `infRuns[0].rank`.
- 재접속: 4000 이 아닌 닫힘이면 백오프(1·2·4·8·16·30초) + visible/online 즉시. 플레이 중 끊김은 180초, **대기실 끊김(새로고침·백그라운드)은 45초** 동안 좌석·방장을 지킨다(`LOBBY_GRACE`). 새로고침은 보드가 없으므로 `dead{r:'reload'}` 후 관전 복귀(`sessionStorage dk_mp` · `dk_mp_run`), 대기실이면 같은 방으로 복귀. 보드 복구는 로드맵.
- 서버가 내 status 를 `left`/`lost` 로 바꾸면 클라는 즉시 관전(서버 판정 우선).

### 상대 요약 카드 `#rivals` · 대기실 `#mp-room` · 로비 `#mp-block`
- 카드 ≤3: 좌석색·이름·배지(`💀 W34` `🏆` `📵 나감` `⌛ 미완료` `↻ 재접속` `⏸ 자리 비움/응답 없음` `x2`/`x3` 배속)·`W♥⚔필드`·보스 HP 바·미니보드(칸 색 `TOWER_DEFS[face].color`, lvl 점). 상대 보드 방향(`sum.o`)이 내 것과 다르면 `remapSpot` 으로 칸을 돌린다. 렌더는 `try/catch` 로 격리하고 `sum` 수신 핸들러·0.5초 틱에서만 돈다(`syncUI`/`update` 밖).
- 자리: 가로(`over`)는 오른쪽 세로 열, 크기는 **가용 높이 ÷ 상대 수 · 트랙 여백 폭**으로(full/mid/slim/line·compact). 세로(`bleed`)는 **아래 띠 오른쪽**(`#log-panel` 왼쪽 46% 회피). `fitStage` 끝에서 `mpLayoutCards()`.
- 로비 `#mp-block`: 이름(`SAVE.name`) · 방 만들기 · 코드로 참가 · **빠른 매칭**. 대기실: 코드 크게 + 복사 · 슬롯 4 · 규칙 문구 · 방장 시작 버튼 · **대기실 채팅 `#mp-chat`**(최근 30줄, 좌석색 이름, 게임 중 채팅도 쌓인다). 빠른 매칭이면 코드 대신 `#mp-queue`(스피너 · `상대를 찾는 중 n/4 · k초 뒤 시작`), 나가기 = 취소, 방이 잡히면 `상대를 찾았습니다 — 전원이 들어오면 바로 시작`.

### 상대 필드 보기 (`mpView` · `VIEW` · `withView`)
- 카드(또는 관전 목록 카드)를 탭하면 `watch{pid}` 를 보내고, 그 플레이어의 `sum.tw`(타워 → `remapSpot` 으로 내 석단에) · `sum.en/ll`(적 → `dist = d / ll × 내 레인 길이`, base 참조로 그림·크기, 체력 0~9) 로 `VIEW.towers/enemies` 를 만든다. 살아 있을 때도 볼 수 있다 — **내 시뮬은 그대로 돈다**(타워는 알아서 싸운다).
- `frame()` 이 `withView(draw)` 로 `S.towers/enemies/projs/beams/fxs/texts/corpses/selTower/heldDie/shakeT/bannerT/glowT/hurtT/mouse` 를 VIEW 것으로 바꿔 그리고 되돌린다. 1초 요약 사이에는 `mpViewAdvance` 가 상대 배속으로 전진시킨다. 캔버스 클릭·드래그·R·1~6 은 뷰 중 무시, HUD 는 숨기고 `#view-bar`(`민수의 필드 · W37 ♥12 · 필드 88 · x2 · 나 W35 ♥9 · [내 필드로]`) 로 대체. 나가기: 내 필드로 · Esc · 상대 탈락/이탈·판 종료 시 자동.

### 남용 방지 · 예산
Origin 검사(불일치 403, 비브라우저는 통과) · IP 버킷(new 10/분, room 60/분, quick 30/분) · 시간당 방 생성 상한 120(Lobby DO 카운터) · 대기열 최대 200 · 코드 정규식은 DO 앞 · 없는 방은 상태 저장 없이 4404 · 소켓당 20/s 버킷 · 4,096 B 초과 1009 · `sum` 엄격 스키마(`en` 은 `[0-9;,]` 만, 악성 값으로 남의 화면이 죽지 않게) · 만료 알람(대기실 15분·종료 10분·판 100분·전원 끊김 3분·claimed 60초·예약 좌석 30초).
무료 플랜: `sum` 2초 주기라 판 내내 DO 활성 ≈ 0.67 객체-시간/판 → **하루 ≈ 40판**(13,000 GB-s/일). 넘으면 그날 멀티만 멈춘다 → Workers Paid $5/월(선형, 판당 ≈ $0.004). 대기실·관전·종료는 하이버네이션.

### 6.6 앱 (Capacitor · `com.fallman.dicekeep` · 주사위 성채)
- `npm run build:www`(`tools/build-www.mjs`) 가 허용 목록으로 `www/` 를 만든다: 코드·`fonts/`·`ui/ vfx/ dice/ props/ map/ towers/ enemies/`·`casual/` 은 코드가 참조하는 파일만(폐기 맵·공격 시트 제외) ≈ 118MB. `index.html` 의 `<!-- APP -->` 에 `app.js`(Capacitor core UMD + App·SplashScreen·StatusBar·SafeArea 플러그인 + 우리 다리)가 끼워진다. `npx cap sync` 가 `www/` 를 `android/app/src/main/assets/public`·`ios/App/App/public` 에 복사(둘 다 gitignore).
- `app.js`(네이티브에서만): `window.DK_NET_URL = wss://dicekeep-net.<acct>.workers.dev` 를 net.js 보다 먼저 두고, `net.js _resolveUrl(…, native)` 가 localhost 규칙보다 앞서 이를 쓴다(`?net=`·저장값은 여전히 우선). 하드웨어 뒤로가기 → `DKAPP.back()`(설정 → 도움말 → 채팅 → 필드 보기 → 타워 선택 해제 → 플레이/관전 나가기 → 각 화면 back → 타이틀/결과 버튼 → 로비는 false → 2초 내 두 번이면 종료, `#toast`). pause/resume → `DKBGM.suspend/resume`. `StatusBar.hide()`, 몰입 모드는 `MainActivity.java`(`WindowInsetsControllerCompat`), 스플래시는 부팅 끝에 `DKAPP_NATIVE.ready()` 로 내린다. 화면 켜짐 유지는 `navigator.wakeLock`(`wakeLockSync`, 웹에서도 동작).
- 서버: `originAllowed` 가 `capacitor://localhost`(iOS)·`https://localhost`(Android, `androidScheme: https`)·`ionic://localhost` 를 허용(`net/test/http.test.js`).
- 아이콘·스플래시 원본 `resources/`(지금은 자리표시자, ART-PROMPTS §7.1~7.3) → `npx @capacitor/assets generate`. 서명·스토어 절차·데이터 보안 답은 `STORE.md`, 빌드 절차는 README.

### 아직 없는 것 (로드맵 M3)
prep '준비 완료' 투표 · 플레이어별 시드 운 스트림(`chest.draw/roll`·`enhanceTower` 에 rnd 주입) · `again` 재대전 · `?room=CODE` 초대 링크 · 새로고침 뒤 보드 복구 · 안티치트(친구 방 전제).

**로그 · 채팅 (`#log-panel`)** — 스타크래프트처럼 화면 왼쪽 아래에 잠깐 남았다가 사라진다.
- `pushLog(text, kind, who)`: `LOG.ttl` 9초 뒤 제거, 마지막 1.2초는 CSS 로 페이드. 최대 8줄.
  종류별 색: `sys` 회색 · `gacha` 금색 · `up` 연두 · `boom` 빨강 · `boss` 주황 · `life` 빨강 · `chat` 흰색(이름은 하늘색).
- `netLog(text, kind)`: 내 화면에 찍고 방 안이면 서버를 통해 같은 방에 중계한다(120자 · 초당 2개, kind 화이트리스트).
- 자동으로 찍히는 것: 유물 이상 뽑기 · ★7 이상 획득 · 확률강화 성공/유지/소멸 · 보스 처치와 보상 ·
  한계선 초과와 목숨 · 보스/고방어 웨이브 시작 · 클리어 · 런 종료 · 스테이지 모드 적 도달.
- **채팅은 멀티에서만**: 게임·관전·필드 보기 중 `Enter` 또는 `#mini-top` 의 💬 버튼(`#chat-btn`, 모바일용)으로 입력창(`#chat-form`)이 열리고, `Enter` 전송 · `Esc` 취소. 서버가 본인에게도 에코한다. 채팅 줄은 좌석색 `●이름`.
  입력 중에는 게임 단축키(R·1~6·Esc)가 막힌다. 대기실에서는 `#mp-chat` 입력칸이 항상 열려 있고(`Enter` 가 포커스), 방이 없으면 `chatOpen()` 이 `false`.
- 훅: `DKlog(text, kind)` · `DKlogs()` · `DKchatOpen()`.

---

## 7. 하지 말 것

- 버튼 굴림(SLOT)과 필드 굴림(DIE) 합치기
- 3D `persp = 10` 낮추기
- file:// 테스트
- 요청 없는 BGM
- 배경 그림에 좌표를 맞추는 방식으로 되돌아가기 (템플릿 규칙: 흙길 한 줄, E 위 칸 비움, 두 번째 길은 흙길 한 칸에만)
- 타워를 길 한가운데 두기
- 걷기 시트 키를 연결만 하고 파일을 빼먹은 채 "완료" 표시하기 (폴백은 되지만 카탈로그가 어긋난다)
- 저장소 루트에 `package.json` 만들기 (정적 Worker 의 빌드 감지가 바뀐다 — Node 도구는 `net/package.json` 에만)
- 루트 `wrangler.jsonc` 에 `main`/Durable Object 넣기 (브랜치 프리뷰 URL 이 사라진다 — 서버는 `net/` 의 별도 Worker)
- content.js 의 웨이브 계수(`INFINITY.wave` 의 count·gap·bosses)를 바꾸고 `net/test/timing.test.js` 패리티 테스트를 안 돌리기
- 서버가 한 번 방송한 웨이브 시각 `T_w` 를 바꾸기 · 멀티 시뮬을 `S.net` 가드 없이 싱글 경로에 섞기
- **순수운빨(clear·multi)에 계정 상태를 들이기** — 무과금·과금 어느 쪽 성장도, 스킨도, 젬도 이 모드의 결과를 바꾸면 안 된다.
  성장 여부의 판단은 `progression.js` `growsIn()` 한 곳뿐이고, 순수 모드의 런 스냅샷은 계정과 무관한 상수다(`pureSnapshot`).
  순수운빨 **입장**도 과금 서비스에 묶으면 안 된다 (`startAccountRun` — 티켓을 못 받으면 계정 보상 없이 그냥 시작한다).
  확인: `npm run test:pure-luck` (무과금 vs 최대 과금 계정의 런이 씨앗 단위로 완전히 동일한지 + 덱빌드는 달라지는지)
- **순수운빨의 난이도(클리어율)를 건드리기** — 웨이브 곡선·뽑기 표·타워 수치·맵은 메운디 이식본 그대로다.
  확인: `npm run test:pure-luck-baseline` (성장·상거래 도입 전 리비전을 별도 포트로 띄워 같은 씨앗으로 대조)

---

## 8. 다음 작업 (우선순위)

1. ~~하드 배경~~ → 납품·레이아웃 작성 완료. `spots2` 가 어색한 맵은 에디터(A 모드)로 손보기
2. ~~걷기 시트 13~36 + 보스 1~10~~ → 납품 완료. 다음 배치는 `NEXT_WALK`/`BOSS_WALK_COUNT` + ART-PROMPTS §2
3. ~~타워 스킨 b~e~~ → 납품 완료
4. ~~밸런스 봇 검증~~ → §9 (3차까지 조정 완료). 실제 손플레이로 티어 5 재확인
5. ~~물 위 석단~~ → 배경 픽셀 물 회피로 해결. 그래도 어색한 맵은 에디터 "자동 석단 굳히기"로 손보기
6. ~~적 25~36 걷기 연결~~ → 완료. 37~ 은 `NEXT_WALK` 에 id 추가 + ART-PROMPTS §2 에 프롬프트 추가
7. 디버그 훅: `DKroll()` 즉시 굴림, `DKplace(idx)` 배치, `DKspots()`, `DKSAVE` — 자동 플레이 봇용
8. **인피니티 아레나 바닥 생성** (ART-PROMPTS §5, 길·석단 없는 바닥만) → 파일만 넣으면 끝. 도로 화풍이 아쉬우면 Grok 타일셋으로 `drawRoad()` 교체
9. 인피니티 밸런스 손플레이 확인 (봇 기준 목표: 무전략 봇이 웨이브 50~70 에서 종료 — 현재 중앙값 59)
10. ~~멀티 1단계 (M1)~~ → 완료: `net/` 서버 + DKNET + 함께하기(방 코드 · 관전 · 순위표 · 재접속). ~~M2~~ → 완료(v3): 개별 진행(배속 x3) · 빠른 매칭 · 상대 필드 보기 · 대기실 채팅. §6.5
11. **출시 배치(2026-09-06)** → 완료: 기기 매트릭스 감사·안전영역·좁은 가로·정사각 화면 · 자체 글꼴 · 오디오 버스·BGM 3트랙·설정 · 획득 연출 아트 훅 · HUD 폴리시 · Capacitor(`android/`·`ios/`) · STORE/privacy. **남은 것**: Grok 그림 납품(아이콘·스플래시·프레임·아이콘 23·VFX, ART-PROMPTS §7) · BGM 파일(MUSIC-PROMPTS) · 실기기 확인(키보드·뒤로가기·펀치홀) · 서명키·스토어 등록 · 채팅 신고/차단(정책 요구 시)
12. **멀티 M3**: 새로고침 뒤 보드 복구(sessionStorage 스냅샷) · `SAVE.mp` 로비 표시 · 관전 중 상대 보드 확대 · 정확한 6초 막간(`INFINITY.spawnEnd` 표) · 예산 실측 후 `SUM_INTERVAL` 조정 · DO 위치 힌트

---

## 9. 밸런스 봇 결과 (2026-09-02)

자동 플레이 봇(`scratchpad/bot2.js`, `DKroll`/`DKplace` 훅 사용, 6배속): 골드가 있으면 굴려서 빈 석단에 놓고, 같은 눈은 합체, 놓을 곳이 없으면 약한 Lv1 을 팔고 교체. 전략이 없는 "무난한 플레이어" 근사치다. 해금 눈: S1~10 은 1~3, S11~20 은 1~5, S21+ 전부.

1차 (티어4 2.05 / 티어5 2.50 / 지수 1.022):

| 스테이지 | 결과 | 남은 목숨 | 타워 |
|---|---|---|---|
| 1 / 6 / 11 | 클리어 | 20 / 20 / 20 | 전부 Lv3 |
| 16 / 21 / 26 | 클리어 | 15 / 14 / 15 | 전부 Lv3 |
| 31 / 36 | 클리어 | 15 / 10 | 전부 Lv3 |
| 41 / 46 | 클리어 | 2 / 5 | 16석단 전부 Lv3, 골드 8~10k 잉여 |
| 50 | **함락** (보스 웨이브) | 0 | 16석단 전부 Lv3 |

→ 티어 5 가 과함. 석단이 다 차고 골드만 쌓이는 구간이라 HP 쪽을 낮췄다: 티어4 1.85, 티어5 2.10, 지수 1.02 (S50 hpScale 7.26 → 5.54).

2차 (위 조정 적용):

| 스테이지 | 결과 | 남은 목숨 |
|---|---|---|
| 36 / 41 / 46 / 48 | 클리어 | 15 / 14 / 15 / 15 |
| 50 (3회) | 클리어 / 클리어 / **함락** | 9 / 15 / 0 |
| 49 | **함락** (보스 웨이브) | 0 |

→ 마지막 웨이브(보스)에서만 무너지는 패턴. 보스 배율 `1.15 + 0.03n` (S50 2.65배) → `1.1 + 0.015n` (S50 1.85배).

3차 (보스 배율 완화, 최종):

| 스테이지 | 결과 | 남은 목숨 |
|---|---|---|
| 46 | 클리어 | 15 |
| 49 | 클리어 | 10 |
| 50 (2회) | 클리어 / 클리어 | 2 / 4 |

판정: 티어 1~3 무피해~소량 누출, 티어 4 목숨 15 안팎, 티어 5 는 10~15, 최종 스테이지는 무전략 봇 기준 2~4 목숨으로 "어렵지만 클리어 가능". 주사위 운에 따른 편차가 커서(같은 스테이지 0~15) 6눈 우선 배치·판매 재배치 같은 실제 전략이 있으면 여유가 더 생긴다. 손플레이 재확인 권장.

### 인피니티 봇 (SP 는 6→5→3→4→2→1 순 자동 투자)

| 설정 | 최강 보드(16×6눈 Lv3, 강화 MAX, 특정 웨이브부터 시작) | 무전략 봇(처음부터) |
|---|---|---|
| 1차: HP 2.2×1.065^w, 보스 개별 보정 포함 | 웨이브 30 에서 함락 (보스 2마리 통과) | 29 (3회 동일) |
| 2차: HP 1.8×1.045^w, 보스 보정 제거 | 47 | — |
| 3차: HP 1.8×1.035^w, 마릿수 ≤36 | 62 | 59 (골드 과잉으로 보드 즉시 최대화) |
| **4차: + 골드 1+0.025w (최종)** | ~60 (60 부터 시작한 기준) | **49 / 69** (2회, 주사위 운 편차) |

판정: 무전략 봇 50~70.

5차 (코드 렌더 나선 아레나, 흙길 2,526px): 무전략 봇 **54 / 54** — 같은 범위 유지, 조정 없음. 후반은 40 부터 시작하는 속도 램프와 마릿수 상한이 벽 역할을 한다. 더 길게 가게 하려면 `INFINITY.wave()` 의 지수(1.035)나 `speedMult` 시작 웨이브부터 조정.

**보드 맵 전환 후 (2026-09-02, 풀파워·석단 15·트랙 1,600px)**

| HP 지수 | 1차 | 2차 |
|---|---|---|
| 1.035 (나선 시절 값) | 75 | 69 |
| 1.04 | 72 | 73 |
| **1.045 (채택)** | **69** | **67** |

트랙이 나선(2,526px)보다 짧아졌지만 보드 15칸이 트랙 양쪽을 모두 덮어 오히려 강해졌고, 6눈까지 전부 나오므로 지수를 1.045 로 올려 목표 50~70 에 맞췄다.

**뽑기 일원화·골드 파워업 후 (2026-09-03)**: 160G 뽑기(일반 50% = 1★)로 평균 타워 질이 떨어져 봇이 32~41 에서 죽었다. 지수 1.05/1.06/1.08 모두 32 — 원인은 HP 곡선이 아니라 **보스 제한시간**: 보스 웨이브 6초 뒤 다음 웨이브가 와서 보스가 잡몹 100+ 속에 묻히고, 웨이브 10 보스의 5분 20초가 웨이브 33~34 에서 끝났다(진단 로그: `oldest: 10`, `bossT` 0 도달). 조치: ① 보스 웨이브는 보스 처치까지 다음 웨이브 보류 ② 방어력 완화(w−30, /6, 고방어 ×4) ③ 소형 마릿수 계수 1.2→1.0. 결과 4판: **39 · 49 · 59 · 69 (중앙값 54)**, 운 좋은 판은 95+. 지수 1.08 유지. 봇은 도박·교환·상성 배치를 하지 않는 하한 기준.

**단일 몬스터 로스터 후 (2026-09-03)**: 지수 1.08 그대로 봇 **60** — 유지. (단일 종·원색이라 렌더가 가벼워져 봇 실행 시간이 705s → 129s 로 줄었다.)

**메운디 시스템 전면 이식 후 (상성·방어력·보스 제한시간·특전, 2026-09-03)**: 지수 1.08 그대로 봇 **55** (상자·도박 미사용) — 목표 안이라 유지.


**두 갈래 분리 + 클리어 확률 조정 (2026-09-04)**: 인피니티를 **도전**(101웨이브 완주 = 클리어, 멀티 예정)과 **무한**(클리어 없음, 싱글 기록)으로 나누고, 도전에만 **최종 관문**(`lateFrom 90`, `lateExp 1.08`)을 걸었다. 검증은 두 갈래로 했다.
- **실제 봇 4판** (무한 곡선, 확률강화·판매 미사용): 59 · 89 · 59 · 49 (중앙값 59) — 목표 50~70 안.
- **순수 Node 몬테카를로** (`scratchpad/sim.js`, content.js 실수치를 뽑아 필드 적체·뽑기·파워업·확률강화를 20,000판): DPS 보정 상수 K 를 실제 봇 결과(중앙값 56~59, 최고 89)에 맞추면 K ≈ 2.6~3.2. 그 구간에서 **101 클리어 0.045% ~ 0.42%**, 아주 후한 K=4.0 에서도 1.72%. 최종 관문이 없으면 같은 K 에서 0.18% ~ 0.81%(K=4.0 은 3.10%)라 벽을 넣어 위쪽을 눌렀다. 봇 정책(뽑기+파워업만)은 20,000판 중 클리어 0회.
- 모델은 상성·방어력·광역·정예·보스 제한시간·필드 한계선을 모두 반영하지만 배치 위치와 사거리는 추상화했다. 클리어 확률은 K 에 민감하므로(±25% → 10배) 위 범위를 그대로 읽는다.
**무한 순환 + 필드 한계선 200 + 사거리 보정 160 + 메운디 갓챠 (2026-09-03)**: 누수 사망이 없어져 지수 1.045 로는 봇이 70+ 에서도 목숨을 하나도 잃지 않았다. 지수별 봇 종료 웨이브: **1.06 → 83 · 1.08 → 60 · 1.10 → 50**. 목표(50~70)에 맞춰 **1.08** 채택. (봇은 상자를 사지 않으므로 갓챠 없는 하한 기준.)

**입구·2.5바퀴·출구 트랙(4,474px) 전환 후 (2026-09-03)**: 지수 1.045 그대로 봇 **69** — 후반 벽은 경로 길이보다 속도 램프·마릿수 상한이라 수치를 유지한다. (그 뒤 무한 순환 룰로 1.08 로 재조정)

**단일 트랙 전환 후 (지름길·하늘길·땅굴 제거, 모든 적이 트랙 한 바퀴)**: 지수 1.045 그대로 **59 / 59**. 공중 적 노출이 늘었지만 지름길 압박이 사라진 것과 상쇄되어 목표 범위 안이라 수치를 유지한다.

### 7.x 순수운빨 클리어율 측정 (2026-09-09)

`npm run measure:clear-rate` (`tools/e2e/pure-luck-clearrate.cjs`) 가 절대 클리어율을 잰다.
`pure-luck-baseline.cjs` 는 "기존과 같은가" 를 보는 대조이고, 이쪽은 "몇 %인가" 를 센다.

봇 정책 세 가지 × 40런 (총 120런) 결과 — **클리어 0회**.

| 정책 | 클리어율 | 중앙값 | 사분위 | 최고 | 종료 사유 |
|---|---|---|---|---|---|
| player (합체·판매 + 파워업 + 확률강화) | 0% (0/40) | 69웨이브 | 49~79 | 89 | 전부 보스 시간초과 |
| greedy (합체·판매) | 0% (0/40) | 69웨이브 | 49~79 | 89 | 전부 보스 시간초과 |
| naive (합체만, 판매 없음) | 0% (0/40) | 39웨이브 | 39~47 | 69 | 시간초과 33 · 목숨 7 |

**벽은 100웨이브 보스다.** 보스 제한시간은 320초인데,

| 웨이브 | 보스 총 체력 | 필요 초당 피해 | 필요한 20★ 3레벨 타워 수 (6눈 파워업 Lv10) |
|---|---|---|---|
| 90 | 7,526,727 | 23,521 | 2.1 / 15칸 |
| 100 | 30,077,756 | 93,993 | **8.6 / 15칸** |

100웨이브 보스는 소형(S)이라 폭발형(7~19★)이 상성 ×0.5 를 받는다. 일반형인 20★(태초)만 온전한
피해가 들어가서, 19★ 3레벨로는 15.1칸이 필요해 15칸 보드로 불가능하다.

그 판을 만드는 비용:
- 뽑기로 20눈을 얻을 확률은 상자당 **0.0992%** — 27개(9타워×3레벨)를 모으려면 약 27,200상자 = **435만 G**
- 확률강화로 8★ 3레벨 하나를 20★ 까지 올리는 기대 비용은 소멸을 무시해도 타워당 약 **93,800 G**
  (고★ 구간 소멸 확률 43~45% 를 반영하면 훨씬 커진다)

한 런의 총 수입은 이보다 한 자릿수 이상 적다. **현재 밸런스에서 101웨이브 완주는 사실상 도달 불가**이며,
클리어율을 올리려면 후반 체력 곡선(`lateFrom` 90 · `lateExp` 1.08), 보스 제한시간(`bossTimeLimit` 320),
경제(상자 160G · 보스 보상), 상성 배수 중 하나를 조정해야 한다.

#### 후반 곡선 조정 후 (2026-09-09)

`lateFrom 90 · lateExp 1.08` → **`lateFrom 60 · lateExp 0.99`**. 손잡이를 하나씩 재서 고른 값이다.

| 조합 | 봇 클리어율 |
|---|---|
| 현재값 그대로 (제한시간 320) | 0% (0/120) |
| lateExp 만 1.04 / 1.00 으로 (lateFrom 90 유지) | 0% (0/16 씩) |
| 제한시간 480 + lateExp 1.00 | 6.3% (2/32) |
| 기본 곡선 1.08→1.07 + lateExp 1.04 | 0% (0/24) |
| 기본 곡선 1.08→1.065 + lateExp 1.04 | 20.8% (5/24) |
| **lateFrom 60 · lateExp 0.99** | **2.3% (6/256)** |

읽을 점 세 가지.

1. **lateExp 만 바꾸는 것은 무의미하다.** 91웨이브부터 걸리는데 아무도 90웨이브 보스를 넘지 못한다.
   1.08 → 1.00 으로 완전히 없애도 도달 웨이브 분포가 한 칸도 안 움직였다.
2. **벽은 두 개다.** 90웨이브는 칼날 위(도달한 최고 판이 필요 피해의 99%)이고, 100웨이브는 6.15배 차이다.
   90을 넘겨야 lateExp 가 비로소 의미를 갖는다.
3. **감산 방식을 골랐다.** 기본 곡선의 밑을 낮추면 1~101 전 구간이 같이 쉬워진다.
   `lateFrom` 을 앞당기고 `lateExp` 를 1 미만으로 두면 시작 웨이브 앞은 그대로 두고 후반만 깎을 수 있다.
   보스 제한시간(320초, 원작 5분 20초)도 건드리지 않았다.

봇은 상한이 아니라 하한이다. 골드 사용은 최적이지만 배치 위치·상성 배치·타워 교체 판단은 하지 않는다.
사람의 클리어율은 이보다 높다.

#### 보스 상성 면제 + 후반 곡선 재조정 (2026-09-10)

**바꾼 것 둘.** ① 보스는 상성 없이 1배로 받는다(잡몹은 그대로). ② `lateExp` 0.99 → 0.9975.

| 조합 | 봇 클리어율 |
|---|---|
| 보스 상성 있음 · lateExp 0.99 | 2.3% (6/256) |
| **보스 상성 없음 · lateExp 0.99** | 14.1% (9/64) |
| 보스 상성 없음 · lateExp 0.995 | 4.7% (3/64) |
| **보스 상성 없음 · lateExp 0.9975 (반영)** | **1.37% (14/1024, 신뢰구간 0.82~2.28%)** |
| 위 + 광역 명중 수정 (2026-09-11, 현재) | **2.0% (5/256, 신뢰구간 0.8~4.5%)** |
| 보스 상성 없음 · lateExp 1.00 | 0.4% (1/256) |

도달 웨이브(1,024런): 중앙값 89 · 사분위 59~89 · 최고 101 · 평균 78.3.
표본을 256런에서 1,024런으로 늘리며 추정치가 1.6% → 1.37% 로 내려앉았고 신뢰구간 상한이 3.9% → 2.28% 로 좁혀졌다.
두 배치가 서로 일관된다(앞 4/256 · 뒤 10/768).

#### 보스를 잡는 데 필요한 판 (2026-09-10)

`npm run measure:boss-board` (`tools/e2e/boss-board-req.cjs`) — 보스 웨이브를 실제로 띄우고
제한시간 안에 죽는지 보며 필요한 최소 타워 수를 이분 탐색한다. DPS 산수가 아니라 사거리·조준·
탄 비행·보스 등장 연출·방어력·특전이 다 반영된다.

6눈 파워업 Lv10 · 석단 15칸 · 현재 밸런스에서 **100웨이브 보스**를 320초 안에 잡는 최소 개수:

| 눈 | Lv1 | Lv2 | Lv3 |
|---|---|---|---|
| 15★ | 불가 | 불가 | 12 |
| 16★ | 불가 | 14 | 10 |
| 17★ | 불가 | 11 | 8 |
| 18★ | 12 | 7 | 4 |
| 19★ | 9 | 5 | 3 |
| 20★ | 6 | 4 | 2 |

**90웨이브 보스**(대형): 15★ Lv3 7 · 16★ 5 · 17★ 4 · 18★ 3 · 19★ 2 · 20★ 2.

읽을 점: 개수보다 **합체 레벨**이 크다. 15★ 는 3레벨이라야 12칸으로 넘어가고, 1·2레벨은 15칸을
다 채워도 모자란다. 상성 면제 전에는 15★ 가 3레벨이어도 15칸으로 불가능했다.

봇은 상한이 아니라 하한이다. 골드 사용은 최적이지만 배치 위치·타워 교체 판단은 하지 않는다.
사람의 클리어율은 이보다 높다.
